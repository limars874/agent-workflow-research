# 步骤1 速览表（10 仓库一句话结论）

| 项目 | 一句话定位 | 最亮的点 | 最大短板 |
|------|-----------|---------|---------|
| **superpowers** | 工程纪律编码为可组合 skills + 六阶段流程 | 对抗性 prompt（预判并驳斥偷懒借口）；跨 10 host 标杆 | 无发布/回滚；安全护栏薄；仪式重 |
| **spec-kit** | 规格是唯一真理来源，宪法→规格→澄清→…→收敛 | /clarify 9 类歧义分类学；需求可追溯闭环；host 耦合极低（30+） | 无独立第二审查；验证证据弱；无复盘 |
| **gsd** | 对抗 context rot：XML 原子计划 + 全新上下文 subagent 执行 | 上下文预算量化到任务级；强制 `<verify>` 命令 | 极重（4:1 编排/代码 token）；深绑 Claude Code；原仓库已归档+治理翻车 |
| **gstack** | 40+ 角色化命令的七阶段"软件工厂"（Garry Tan） | 跨模型对抗审查（Codex 二审）；验证下沉到机器（持久 Chromium 守护） | 无小步切片；Build 阶段无实现纪律 |
| **trellis** | 分层 spec + 记忆持久化进仓库，hook 强制注入上下文 | commit 前强制 spec 回写（契约级沉淀）；单一真源编译到 16 host | 无外部审查；止于 commit；深依赖 CC hooks |
| **web-dev-skills** | Rust+React 全生命周期串行门禁流 | 无时间戳 .state.json 无状态恢复；protocols 单一真相源 | 绑定特定技术栈；纯串行；社区小 |
| **consensus-rnd** | 三个有偏 solver + meta-judge 真值表收敛，GitHub 即数据库 | 共识机械化为可测试契约；prompt 行为用 pytest 锁定 | 无意图澄清/切片；发布无回滚；极重 |
| **maestro-flow** | Ralph 11 状态 FSM 全生命周期编排 + 自增强知识图谱 | 完成态只能由 CLI 写入（LLM 不能自证完成）；漂移检测熔断 | 无发布/沙箱；8 万行超重平台 |
| **missions** | CSV 为唯一状态源的长任务闭环执行 | 反暂停护栏体系；验收合同化（反 mock 糊弄） | 无前段（澄清/规格）；零机器验证 |
| **helloagents** | 三 host 重量级增强层，落盘证据强制交付闭环 | Stop hook 门禁 + 反过早停止设计最系统 | 含 No-Refusal 条款有合规风险；黑箱重 |
| **mattpocock-skills**（追加） | 反框架的"工程判断力"工具箱，状态外包 issue tracker | grilling 对齐工艺 + tdd/diagnosing-bugs 验证方法论全生态最深；skill 当软件维护（changesets） | 机器强制≈零（3 个脚本）；执行编排薄；157k star 含 KOL 光环 |

## 已浮现的归纳线索（步骤2 原材料）

1. **生态普遍空白**：几乎所有框架止于 commit/PR，缺"可控发布/回滚"
2. **复盘沉淀闭环**只有 trellis（spec 回写）和 maestro-flow（知识图谱）做成
3. **新维度候选："完成权剥夺"** —— LLM 不能自证完成（maestro CLI 写完成态、helloagents Stop hook 门禁、missions 反暂停护栏）。用户候选概念清单中没有，值得独立成维度
4. **轻重光谱**：missions/web-dev-skills 轻 ←→ gsd/maestro-flow/helloagents 重；"重"普遍来自把 FSM 逻辑写进 markdown 靠 LLM 自律
5. **机器强制 vs prompt 自律**是区分框架成熟度的关键分水岭（Runtime/Scripts 下沉维度的证据）
