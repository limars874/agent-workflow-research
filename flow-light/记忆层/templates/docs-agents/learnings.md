<!-- 教训沉淀(抄 helloagents hello-reflect)。放 docs/agents/learnings.md。
     触发条件(满足任一才记):2+ 次验证才过 / 调试 >3 次才修 / 中途换了方案 / 用户纠正了错误假设。
     格式:- [日期] 教训 — 背景:什么有效/什么失败(≤2 行)。追加不重写。
     要求:抽象指导,不是具体代码模板(具体模板会让 AI 锚定错细节)。只记真正有价值的。
     稳定、通用的教训,升级成 constraints.md 里的约束(让它每任务强制生效)。 -->

# 教训

- [2026-01-01] <示例:重构前先把现有行为列成清单再动手> — 背景:上次重构漏了空值分支,因为没先盘点已有行为
