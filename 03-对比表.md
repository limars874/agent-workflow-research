# 步骤3：11 框架 × 16 维度能力覆盖对比表

> 评分口径：`02-能力框架.md` §4（L0 缺失 / L1 Prompt 自律 / L2 状态落盘 / L3 机器强制，可加半级）。
> 判级看**最弱链**：关键路径靠 prompt 自律即使有落盘也降级。L3 ≠ 好，须与臃肿度合看。
> 每格评分均可回溯到 `01-仓库研究/` 对应报告的具体机制；证据不足处标 `?`。

---

## 一、主矩阵（11 × 16，mattpocock-skills 为第二轮追加）

| 维度 | superpowers | spec-kit | gsd | gstack | trellis | web-dev-skills | consensus-rnd | maestro-flow | missions | helloagents | mattpocock |
|---|---|---|---|---|---|---|---|---|---|---|---|
| **S1 澄清意图** | L2 | **L2+** | L2 | L2 | L2 | L2 | L1 | L2 | L1 | L1+ | L1+ |
| **S2 写清规格** | L2 | **L2+** | L2 | L2 | **L2+** | L2 | **L2+** | L2 | L2 | L2 | L2- |
| **S3 小步切片** | L2 | L2 | L2 | L1 | L2 | **L2+** | L1 | L2 | L2 | L2 | L2- |
| **S4 实现功能** | L2 | L2 | L2 | L1 | L2 | L2 | **L2+** | L2 | L2 | L2 | L1 |
| **S5 验证证据** | L1+ | L1 | L2 | **L3** | L2 | L2+ | **L3** | L2 | L1+ | L2+ | L1+ |
| **S6 独立审查** | L2 | L1 | L2 | L2+ | L1+ | L2 | **L3** | L2 | L1+ | L1 | L1+ |
| **S7 可控发布** | L1 | L1 | L1 | L2 | L1 | L2 | **L2+** | L1 | L1 | L2 | L0 |
| **S8 复盘沉淀** | L1 | L0 | L1+ | L2 | **L2+** | L1 | L1+ | **L2+** | L0 | L2 | L1+ |
| **C1 任务状态** | L2 | L2 | L2+ | L2 | L2+ | L2+ | **L3** | L2+ | L2 | L2+ | L2 |
| **C2 上下文治理** | L2 | L2 | L2 | L2 | **L3** | L2 | L2+ | L2+ | L1 | L2+ | L1+ |
| **C3 记录追溯** | L1+ | L2 | **L2+** | L2 | L2 | L1+ | **L2+** | **L2+** | L2 | L1+ | L2- |
| **C4 安全护栏** | L1 | L1 | L1+ | L2 | L1+ | L1+ | **L3** | L1 | L0+ | **L3** | L2+ |
| **C5 完成权剥夺** | L1 | L1 | L2 | L1+ | L2 | L2 | **L3** | **L3** | L1+ | **L3** | L1 |
| **C6 跨host可移植** | **L3** | **L3** | L1+ | L2 | L2+ | L1+ | L1+ | L1 | L2 | L2+ | L1+ |
| **C7 机器下沉** | L2 | L2 | L1+ | **L3** | L2+ | L2+ | **L3** | L2 | L0 | **L3** | L1 |
| **C8 无人值守/并行** | L1 | L1 | L2+ | L1 | L1 | L1+ | **L3** | L2+ | L2 | L2 | L1? |

---

## 二、逐框架评分依据（仅列非显然格子）

### superpowers
- S5 = L1+（非 L2/L3）：Gate Function / Iron Law 是纯 prompt 纪律，无证据落盘/机器判定，最弱链在自律。
- S6 = L2：fresh reviewer subagent + `review-package` 脚本生成 diff 包（有脚本、判决仍 LLM）。
- C5 = L1：verification-before-completion 是 prompt 级鼻祖，"If you lie, you'll be replaced" 无机器后果。
- C6 = L3：10 host 薄适配层 + `porting-to-a-new-harness.md` 正式移植规范 + `.version-bump.json` 版本同步自动化。
- C4 = L1：仅流程护栏，报告明言"几乎没有破坏性操作层面护栏"。

### spec-kit
- S1 = L2+：`/clarify` 9 类歧义分类学 + 答案原子写回 spec（落盘），最多 5 问硬上限。
- S2 = L2+：FR-###/SC-### 编号 + `check-prerequisites.sh` 前置校验下沉脚本 + `/converge` 反向对照。
- S5 = L1：报告明言"验证证据薄弱，TDD 靠 prompt 约束，无强制跑测试出证据机制"。
- S6 = L1：analyze/converge 均为同 agent 自审，无第二视角。
- S8 = L0：无 retro 命令，宪法不自动演化。
- C6 = L3：单模板源占位符编译到 30+ host，bash/PowerShell 双脚本。

### gsd
- S4 = L2：偏差四规则、分支 deny-list 均写在 executor prompt 里，落盘 SUMMARY 但拦截非机制层。
- C1 = L2+：STATE.md 唯一事实源 + gsd-tools SDK 结构化读写（state.advance-plan 等）。
- C3 = L2+：SUMMARY.md deviations 逐条对应 commit hash。
- C5 = L2：`<verify>` 强制 + `gate="blocking-human"` 不可跳过，但完成态仍由 LLM 写文件。
- C6 = L1+：报告明言"多 runtime 支持是安装器层适配，非架构层抽象"，深绑 Claude Code 语义。
- C7 = L1+：有 SDK 但"大量编排逻辑仍在长 prompt 里"，最弱链定级。

### gstack
- S3 = L1：报告明言"无显式任务切片/stacked-PR 机制，大特性靠人肉切"。
- S4 = L1：Build 阶段有意留白，裸实现无纪律。
- S5 = L3：Bun 编译持久 Chromium 守护进程，验证是机器事实非模型口述。
- S6 = L2+：跨厂商双模型审查 + pre-emit gate（引不出代码行即降置信）+ Codex P1 阻断——门禁判定仍在 prompt 层故非 L3。
- S7 = L2：唯一做全 ship→land-and-deploy→canary 链路，但编排靠 skill prompt，无回滚。
- S8 = L2：learnings.jsonl 被其他 skill 前置查询（"Prior learning applied"），查询动作靠 prompt 故非 L2+。
- C4 = L2：/careful /freeze 是 prompt 级，但浏览器侧有 BERT-small ONNX 注入分类器等机器防御。

### trellis
- C2 = L3：SessionStart/UserPromptSubmit/PreToolUse 三层 hook 强制注入，"injected via hook, not recalled from memory"。
- S8 = L2+：spec-sync 是 commit 前置条件 + trellis-update-spec 七段可执行契约模板；前置门禁本身在 prompt（workflow.md）故差半级到 L3。
- S6 = L1+：trellis-check 是干净上下文 subagent 但同框架自查，报告列为"明显欠缺"。
- C1 = L2+：task.py 机器状态 + 每轮注入 workflow-state 面包屑。
- C6 = L2+：单一真源编译 16 平台 + 能力降级策略，但"灵魂在 Claude Code"。

### web-dev-skills
- S3 = L2+：phase/slot/item 三层拆分 + DAG 无环硬校验、缺字段直接拒写。
- C1 = L2+：`.state.json` 刻意零时间戳，纯 status 枚举+文件存在性无状态重建。
- S7 = L2：t-push 本地 CI 脚本 + t-release + t-prd-publish 文档回流（无远程 PR/回滚）。
- C4 = L1+：runtime-boundaries 协议 + accept 只读均为 prompt 约定，无 hook 拦截。
- C6 = L1+：跨 host 靠 dispatcher hack + 手动角色全文注入，"能用而非一等公民"。

### consensus-rnd
- S1 = L1：报告明言"几乎没有人向 agent 澄清意图的交互环节"，仅 sshx intake GoalArtifact。
- S4 = L2+：隔离 worktree + Spawn Claim 原子锁 (#490) 防重复派发。
- S5/S6 = L3：完成认定只看退出码+sentinel+可解析 envelope（"日志文本绝不参与"）；合并真值表 `reject==0 && approve>=1 && head 一致` 被 pytest 锁定。
- S7 = L2+：Release Publisher exact-SHA required-checks 全绿才发布（CI 机器门禁），但明言"不回滚"，差半级。
- C1 = L3：状态全在 GitHub labels/comments，ActiveControllerLease 跨设备唯一 controller。
- C4 = L3：全面 fail-closed + controller 不读 prose 防注入 + "No public lifecycle CLI"。
- C6 = L1+：四平台 manifest 是真跨 host，但运行时深绑 "Claude Code controller + Codex worker + gh CLI + GitHub"。

### maestro-flow
- C5 = L3：invariant 6——`completion_confirmed=true` 只能由 CLI `maestro ralph complete` 写入。
- S8 = L2+：retrospective 洞见三路由 → 17 hooks 自动注入下次执行（回写+自动注入双环）；路由判定在 prompt 故差半级。
- S4 = L2：每 3 步 re-grounding 门 + A_REGROUND_HALT 熔断——但 11 状态 FSM 写在 markdown 靠模型自律，最弱链降级。
- C7 = L2：maestro CLI 下沉状态/KG 查询，但核心 FSM 未落代码（报告列为可靠性风险）。
- C6 = L1：深绑 Claude Code（.claude/ + 17 hooks），Gemini/Codex 只是被 delegate 的"手"。
- C4 = L1：危险命令防护仅 shell-exec-protocol.md 约定层面。

### missions
- S5 = L1+：required_mcp 验收合同 + 管线 vs 模块判定设计极细，但零脚本，证据核查全靠 LLM 自觉（最弱链）。
- C5 = L1+：反暂停三问断言、continue_now、"非终态 turn 必须以工具调用结尾"——最系统的纯 prompt 级实现。
- C1 = L2：CSV 唯一状态源 + mission-recovery 修复不一致，但修复者仍是 LLM 读 prompt。
- C4 = L0+：几乎无破坏性操作防护，且反暂停狂奔与安全存在天然张力（仅 approved-doc 批准门）。
- C7 = L0：14 个 markdown 零脚本零代码，报告明言"状态漂移无机器校验"。
- S8 = L0：review.md/log.md 是过程记录，无沉淀回路。

### helloagents
- C4 = L3：guard.mjs 挂 PreToolUse 三层拦截（命令→语义→外部输出）⚠️ 但 No-Refusal 条款是负资产，采用须剔除。
- C5 = L3：Stop/SubagentStop hook 上 turn-stop-gate/ralph-loop，"无落盘 JSON 回执不算完成"是机器判定。
- S5 = L2+：qa-review.json 落盘回执 + 验证命令输出强制；审查判定本身仍是 LLM。
- S6 = L1：qa-review 由同一 agent 收口，报告明言"缺 fresh-context 独立 reviewer"。
- C6 = L2+：sync-hosts 投影三 host（各带 hooks/plugin/doctor），host 无 hooks 则守卫退化为纯 prompt。
- S7 = L2：delivery-gate.mjs + auto_commit 仅在验证通过后提交（止于本地 commit）。

### mattpocock-skills（第二轮追加，详见 01-仓库研究/mattpocock-skills.md）
- S1 = L1+（非更高）：grilling 工艺全生态最深（一次一问+每问附推荐答案+查码优先），但"共识前 hold implementation"纯 prompt，批准信号不落盘——**craft 深度与 enforcement 等级是两回事**，它是这条口径最极端的例证。
- S5 = L1+：tdd（seam、反 tautological 测试）与 diagnosing-bugs（feedback-loop-first）是最深的验证**方法论**，但零证据落盘门禁。
- C4 = L2+：有全仓唯一真 L3 组件（PreToolUse 拦截危险 git，exit 2 BLOCKED），但可选安装、仅覆盖 git、仅 Claude Code——覆盖面即最弱链。
- C1 = L2：状态外包 issue tracker（五态标签状态机 + assign=claim），跨 session 可恢复，无本地校验。
- S7 = L0、C5 = L1、C7 = L1：止于 commit；无 Stop hook/回执；全仓 3 个 shell 脚本，triage FSM 写在 markdown。
- C8 = L1?：wayfinder（tracker 原生 claim 锁）/loop-me 均在 in-progress 孵化桶，未正式发布。

### C8 无人值守/并行 评分依据（第二轮追加维度，定义见 02-能力框架.md C8）

> 只看跨任务能力：无人值守（中途门禁可否降级、受阻自处理）+ 多任务并发协调（认领、隔离、沉淀合并）。任务内 fan-out（wave/[P]/specialist dispatch）不计入。

| 框架 | 评分 | 依据 |
|---|---|---|
| superpowers | L1 | 有 worktree/并行派发 skill 等工具件，但流程灵魂是人机乒乓（一次一问、批准前禁实现），无多任务协调 |
| spec-kit | L1 | [P] 仅任务内；implement 遇未完成 checklist **暂停询问用户**，无人值守卡死 |
| gsd | L2+ | auto 模式分级降级范本（唯 `gate="blocking-human"` 不可跳过）+ wave 零文件交集并行 + worktree 边界断言 |
| gstack | L1 | 单实例角色切换非真多 agent，并行明确外包给 Conductor 等外部工具 |
| trellis | L1 | 0.5.0 主动移除 /parallel + Multi-Agent Pipeline；task 目录隔离留了地基，但 consent 门禁为陪伴设计 |
| web-dev-skills | L1+ | 显式单流（"任意时刻最多一个 running"，用可控性换速度）；无状态恢复设计使单流续跑可靠 |
| consensus-rnd | **L3** | 全套协调基建：Spawn Claim 原子锁 (#490)、ActiveControllerLease、隔离 worktree、CODEX_FLOOR 并发配置、并发监控 pytest、fail-closed |
| maestro-flow | L2+ | auto_confirm + ralph loop + Delegate/Team/Wave/Swarm；完成态 CLI 独占恰好补无人监督；但协调逻辑多在 prompt |
| missions | L2 | 单流无人值守的 prompt 级天花板（反暂停护栏全套），但 CSV 单状态源结构性单流 + C4≈L0 裸奔 |
| helloagents | L2 | Stop hook ralph-loop + "受阻先试 2 种替代方案"自处理协议；多任务仅 repo-shared 知识库共享，无认领协调 |

---

## 三、附加评价表

| 框架 | 臃肿度 | host 耦合 | 接入成本 | 适用场景一句话 |
|---|---|---|---|---|
| superpowers | 中（本体轻、运行时 token 重） | 低 | 低 | 通用工程纪律底座，任何 host 上的"默认好习惯" |
| spec-kit | 重（每 feature 5+ 文档） | 极低 | 低 | 中大型 feature 的规格驱动开发，跨工具团队标准 |
| gsd | 重（4:1 编排 token，70 命令） | 高（CC 深绑） | 中高 | 大型 greenfield 项目全程托管；⚠️ 治理翻车需选社区版 |
| gstack | 重（40+ skill + 守护进程） | 中低 | 中高 | Web/iOS 全栈创业项目，需要真实浏览器验证与发布链 |
| trellis | 重（脚本运行时 + 多产物） | 中（接口跨平台、灵魂在 CC） | 中 | 多人长期仓库的规范沉淀与上下文注入；AGPL 注意 |
| web-dev-skills | 偏重（栈深绑定） | 中偏高 + 栈耦合深 | 高 | 仅 Rust+React（+小程序）团队全盘采纳 |
| consensus-rnd | 极重（daemon 群 + 45 变量 + 百测试） | 高（GitHub+Codex+CC 组合） | 极高 | 全自治仓库研发实验；sshx 子集可单独轻用 |
| maestro-flow | 极重（8 万行平台） | 高（CC + 自家 CLI + SQLite） | 中（安装易、迁出难） | all-in 一套流程的重度长期项目 |
| missions | 极轻（14 个 markdown） | 低（Codex/CC 双入口） | 极低 | 已有清晰需求的长任务断点续跑，配前段框架用 |
| helloagents | 重（73 脚本黑箱运行时） | 中（三 host 投影，依赖 hooks） | 中（信任成本高） | 个人重度使用者的交付门禁层；⚠️ No-Refusal 合规风险 |
| mattpocock-skills | 极轻（纯 markdown + 3 脚本，单 skill 可拆用） | 中（名义通用，hook/plugin 实绑 CC；深依赖 gh/glab tracker） | 低（npx 一键 + 三问 setup） | 会自己编排流程的资深工程师做人机结对的对齐与验证；不适合无人值守/弱模型/自动驾驶诉求 |

---

## 四、矩阵读出的结论（供步骤 4 文章用）

### 4.1 维度冠军（列冠军）

| 维度 | 冠军 | 冠军机制 |
|---|---|---|
| S1 澄清意图 | **spec-kit** (L2+) | /clarify 9 类歧义分类学 + 原子写回 |
| S2 写清规格 | **spec-kit** (L2+) | FR/SC 编号 + converge 反向对照闭环（trellis/consensus-rnd 并列半席） |
| S3 小步切片 | **web-dev-skills** (L2+) | DAG 无环硬校验、缺字段拒写 |
| S4 实现功能 | **consensus-rnd** (L2+) | 隔离 worktree + spawn 原子锁 |
| S5 验证证据 | **gstack / consensus-rnd** (L3) | Chromium 守护进程机器事实 / 退出码+sentinel |
| S6 独立审查 | **consensus-rnd** (L3) | 互盲三 reviewer + pytest 锁定合并真值表 |
| S7 可控发布 | **consensus-rnd** (L2+，覆盖最全是 gstack) | exact-SHA required-checks；gstack 唯一有 canary |
| S8 复盘沉淀 | **trellis / maestro** (L2+) | spec-sync commit 前置 / 洞见三路由 + hook 回注 |
| C1 任务状态 | **consensus-rnd** (L3) | GitHub 即数据库 + controller lease |
| C2 上下文治理 | **trellis** (L3) | 三层 hook 强制注入 |
| C3 记录追溯 | **gsd / consensus-rnd / maestro** (L2+) | deviations↔commit hash / GitHub 全留痕 / decisions.ndjson |
| C4 安全护栏 | **helloagents / consensus-rnd** (L3) | PreToolUse 三层拦截 / 全面 fail-closed |
| C5 完成权剥夺 | **maestro / helloagents / consensus-rnd** (L3) | CLI 独占写完成态 / Stop hook 门禁 / sentinel 判定 |
| C6 跨host可移植 | **superpowers / spec-kit** (L3) | 移植规范+10 host / 单源编译 30+ host |
| C7 机器下沉 | **consensus-rnd / gstack / helloagents** (L3) | pytest 锁 prompt / Bun 守护进程 / 73 个 .mjs |
| C8 无人值守/并行 | **consensus-rnd** (L3) | 认领原子锁 + controller lease + 隔离 worktree 全套协调基建（独一档；次优 gsd/maestro 的分级降级挡位） |

### 4.2 框架能力剖面（行形状）

| 框架 | 剖面形状 |
|---|---|
| superpowers | **均衡中型 + 可移植单尖峰**：主流程全 L2 上下，唯 C6 独占 L3；后端（S7/S8/C4/C5）塌陷 |
| spec-kit | **前段双峰**：S1/S2/C6 顶尖，S5 之后断崖（S5=L1、S6=L1、S8=L0）——"只管想清楚，不管做到位" |
| gsd | **中段厚实的均衡重型**：无 L3 尖峰、无 L0 空洞，全靠落盘文档撑 L2 面；机器下沉不足是短板 |
| gstack | **后段尖峰**：S5/S6/S7/C7 领先，S3/S4 反而 L1——"两头门禁夹裸实现"的哲学直接写在分数里 |
| trellis | **记忆型**：C2=L3、S8=L2+ 是全场唯一"注入+回写"双环；审查/发布弱 |
| web-dev-skills | **窄而深**：切片与状态契约做到 L2+，代价是栈绑定；小社区但设计干净 |
| consensus-rnd | **后段全 L3 重装**：S5-S7、C1/C4/C5/C7 大满贯，但 S1/S3 是全场最弱之一——无人形（没有人机交互前段） |
| maestro-flow | **C5 单点 L3 + 全面 L2 平台**：完成权剥夺的发明者之一，但 FSM 在 markdown 拖累多格半级 |
| missions | **单点尖峰轻量**：C1/C5 用纯 prompt 打出超越体量的分数，C4/C7/S8 三格 L0 级 |
| helloagents | **门禁三峰**：C4/C5/C7 全 L3（hook 机器强制最系统），但 S6 独立审查 L1 是明显洼地 |
| mattpocock-skills | **全场最平的 L1-L2 面 + craft 纵深**：L 级矩阵上无一尖峰，但 S1/S5 的工艺深度是矩阵度量不到的——它证明 L 级衡量的是强制力不是判断力 |

### 4.3 生态空白验证

- **S7 可控发布**：符合预期——10 家中 6 家 L1、无一家 L3；唯一 L2+（consensus-rnd）还明言"不回滚"。**回滚/灰度预案是全生态零覆盖**（gstack /canary 只是监控不是回滚）。
- **S8 复盘沉淀**：符合预期——2 家 L0、3 家 L1 档；达到"回写+自动注入"双环判据的只有 trellis 和 maestro（均 L2+，仍无 L3）。
- 附加发现：**S6 独立审查两极分化**——要么 L3 级对抗设计（consensus-rnd/gstack），要么退化为自审 L1（spec-kit/helloagents/trellis），中间形态少。

### 4.4 互补组合建议线索

| 组合 | 拼接逻辑 | 覆盖效果 |
|---|---|---|
| **spec-kit + missions**（作者 codex-missions.md 已半官方推荐 superpowers 版） | spec-kit 补 missions 全缺的 S1/S2 前段，missions 补 spec-kit 弱的 C1/C5 执行闭环 | 轻量、host 耦合都低，全流程 L2 面 |
| **superpowers + gstack** | superpowers 补 gstack 的 S3/S4 实现纪律，gstack 补 superpowers 的 S5(L3)/S7/C4 后段 | 15 维中 13 维 ≥L2，唯 S8/回滚仍缺 |
| **trellis + gstack /codex 二审** | trellis 补上下文治理与沉淀（C2=L3/S8），外挂 gstack 跨模型审查补 trellis 的 S6=L1+ | 长期仓库最佳记忆+审查组合 |
| **任意组合 + helloagents guard.mjs 思路** | C4=L3 的 PreToolUse 拦截是全场唯一可单独抽取的安全件（剔除 No-Refusal） | 补全生态普遍的 C4 洼地 |
| 单点借鉴 | maestro invariant 6（CLI 独占完成态）、consensus-rnd pytest 锁 prompt、web-dev-skills 无时间戳 .state.json | 不引全家桶只抄机制 |

**总结论**：没有任何单一框架在 15 维上超过 5 个 L3；"全能"不存在，存在的是四种原型——前段规格型（spec-kit）、执行闭环型（missions/gsd）、后段门禁型（gstack/consensus-rnd/helloagents）、记忆沉淀型（trellis/maestro）。步骤 4 文章的骨架即：按失败模式选原型、按机制（而非框架）做拼装。
