# 步骤2：Agent 编码 Workflow 统一能力框架

> 素材：`00-开篇.md`、`01-速览表.md`、`01-仓库研究/` 10 篇深读报告。
> 框架 = **8 步主流程（步骤流）× 8 项横切能力 × 4 级成熟度**（C8 为第二轮讨论追加）。
> 核心结构调整：把用户候选的"治理上下文"从步骤流移入横切能力（理由见 §3），并新增"完成权剥夺"为独立横切维度（裁决见 §2.5）。

---

## 一、最终步骤流（8 步，Pipeline）

> 每步给出：针对的失败模式、≥2 个框架证据、机器可强制部分。步骤是"主线时序"，横切能力（§2）贯穿全程。

### S1 澄清意图（Clarify）
| 项 | 内容 |
|---|---|
| 失败模式 | agent 拿到模糊需求直接写码；"合理化借口"跳过提问（superpowers 明文对抗的第一失败模式） |
| 证据 | **spec-kit** `/clarify`：9 类歧义分类学 + Impact×Uncertainty 排序 + 最多 5 问一次一问 + 答案原子写回 spec；**superpowers** brainstorming：一次一问 + 2-3 方案权衡 + "用户批准设计前禁止任何实现动作"硬门禁；**gstack** `/office-hours` YC 式拷问；**trellis** brainstorm 的 Evidence Rule（能查代码库的绝不问用户）；**maestro** brainstorm 五阶段访谈 |
| 机器可强制 | 弱——提问质量无法机验。可强制的是**门禁**：批准信号未落盘则禁止进入实现（trellis task-creation consent、helloagents "用户确认计划前禁止写实现代码"） |

### S2 写清规格（Spec）
| 项 | 内容 |
|---|---|
| 失败模式 | 意图只存在于对话历史，session 一断即失真；需求无编号无法追溯 |
| 证据 | **spec-kit**：spec 是唯一真理源，FR-###/SC-### 编号 + requirements checklist 自校验 ≤3 轮 + `/converge` 实现后反向对照（missing/partial/contradicts/unrequested）；**gsd** REQUIREMENTS.md + goal-backward must-haves 倒推；**missions**：规格即 CSV，acceptance_criteria 强制 "WHEN X THEN Y; ref: file:line" 机器可验格式；**trellis** prd.md 必需 + 可测试验收标准 |
| 机器可强制 | 格式校验（missions 19 列 schema、gsd XML 四要素缺项拒写）；需求-任务覆盖矩阵（spec-kit analyze） |

### S3 小步切片（Slice）
| 项 | 内容 |
|---|---|
| 失败模式 | 一次改一大片，无法独立验证/审查/回滚；context rot 中途质量崩塌 |
| 证据 | **superpowers**：2-5 分钟任务粒度，以"reviewer 可独立批准/拒绝"为切分标准；**gsd**：每计划 2-3 任务 + wave 依赖并行 + "vertical slices over horizontal layers"；**helloagents** tasks.md："每个任务端到端可验证，厚任务必须切薄"；**maestro** plan.md 反过度切分（"one feature = one task"）——切片有上下界 |
| 机器可强制 | DAG 无环硬校验（web-dev-skills t-task 循环依赖直接拒写）；文件数/LOC 阈值触发拆分（gsd >5 文件即拆） |

### S4 实现功能（Implement）
| 项 | 内容 |
|---|---|
| 失败模式 | 执行偏离计划无人察觉；遇阻静默降级/静默缩范围；污染主上下文 |
| 证据 | **gsd** executor 偏差四规则（Rule1-3 分级自动修、Rule4 架构变更必须问人、3 次上限、全记 SUMMARY）+ "No silent scope reduction"；**superpowers** subagent-driven：每任务全新 subagent + task-brief 最小上下文；**maestro** 每 3 个执行步插 re-grounding 门，drifted 高置信直接熔断；**helloagents** "受阻前必须尝试 2 种实质不同替代方案" |
| 机器可强制 | worktree/分支边界断言（gsd deny-list main/master）；漂移熔断绕过 auto_confirm（maestro A_REGROUND_HALT） |

### S5 验证证据（Verify）
| 项 | 内容 |
|---|---|
| 失败模式 | "Should work"式口头宣称；用 mock/stub/dry-run 伪装真实完成 |
| 证据 | **superpowers** Gate Function："跑命令→读完整输出和 exit code→才能声明"，"Evidence before claims, always"；**gsd** 每任务 `<verify>` 强制 <60s 自动化命令，"Manual-only checks are forbidden"；**missions** required_mcp 验收合同 + "管线 vs 模块"判定（端到端验收禁止 import 内部模块绕过）+ 每工具最低证据门槛；**gstack** `/qa` 真实 Chromium 点测截图；**maestro** verify.md 禁语列表 + IDENTIFY→RUN→READ→VERIFY 单消息证据链 |
| 机器可强制 | 这是最应机器化的一步：证据文件落盘才算数（helloagents "无 qa-review.json 不得宣称收口"）；只认退出码+sentinel、日志文本无效（consensus-rnd） |

### S6 独立审查（Review)
| 项 | 内容 |
|---|---|
| 失败模式 | 自己审自己，同一上下文同一偏见，看不见自己的盲点 |
| 证据 | **gstack** `/review`+`/codex` 跨厂商双模型对抗审查，Codex 发现 P1 阻断 ship；置信度校准（finding 必须引用具体代码行否则降级）；**consensus-rnd** 三个有偏 reviewer + 机械合并真值表 `reject==0 && approve>=1 && head 一致`，同轮互盲；**superpowers** 每任务 fresh reviewer subagent + fix 循环 + 最强模型整分支终审；**missions** Vision Review 独立 sub-agent 对照批准文档查 claim/evidence 对齐 |
| 机器可强制 | 合并真值表（consensus-rnd 全套 pytest 锁定）；review head == PR head 校验；P1 门禁阻断 |

### S7 可控发布（Release）
| 项 | 内容 |
|---|---|
| 失败模式 | 生态最大空白（速览表线索 1）：几乎所有框架止于 commit/PR，无部署/灰度/回滚 |
| 证据 | 做得最全的仅 **gstack**：`/ship`（测试门禁→PR）→`/land-and-deploy`（合并+部署+健康检查）→`/canary` 上线后监控；**consensus-rnd** Release Publisher：exact-SHA required-checks 全绿才发布（但明言"不回滚"）；其余：superpowers 止于分支合并、spec-kit/trellis/maestro/missions 均止于 commit/里程碑收口 |
| 机器可强制 | 天然最可机器化（CI required checks、健康检查、金丝雀指标），恰恰因此被"prompt 框架"们集体外包给了 CI——但**回滚预案**仍无人做，是本框架保留此步的理由 |

### S8 复盘沉淀（Retro）
| 项 | 内容 |
|---|---|
| 失败模式 | 每次会话从零开始，同一个坑反复踩；教训留在聊天记录里蒸发 |
| 证据 | 闭环只有两家做成（速览表线索 2）：**trellis** spec-sync 是 commit 的**前置条件**（不沉淀不许提交），trellis-update-spec 七段可执行契约模板；**maestro** retrospective 洞见三路由（specs/issues.jsonl/learnings.md）→ 17 hooks 自动注入下次执行，"Workflows generate knowledge, knowledge improves future workflows"；次一级：**gstack** `/learn` learnings.jsonl 被其他 skill 前置查询、**helloagents** hello-reflect 有明确触发条件（2+ 验证循环/调试>3 次/换方案） |
| 机器可强制 | 沉淀作为 commit/archive 的前置门禁（trellis）；沉淀物被 hook 自动注入而非靠模型记忆（maestro、trellis）——"回写+自动注入"双环都在才算闭环 |

---

## 二、最终横切能力（8 项，Cross-cutting）

### C1 任务状态（Task State）
- **失败模式**：断线/compaction 后不知身在何处，从头再来或重复劳动。
- **证据**：**missions** CSV 唯一状态源 + mission-recovery 修复不一致状态（招牌能力）；**web-dev-skills** `.state.json` 刻意零时间戳，纯 status 枚举+文件存在性+DAG 无状态重建；**consensus-rnd** 状态全在 GitHub labels/comments，本地仅 cache；**superpowers** progress.md 台账 + git commit 锚点。
- **机器可强制**：状态读写下沉 CLI/脚本（maestro `ralph continue`、trellis task.py）；状态 schema 校验与自动修复（missions recovery）。

### C2 上下文治理（Context Governance）★由步骤流移入
- **失败模式**：context rot——窗口填满后质量降解；规则散落多处漂移；子 agent 读不到该读的。
- **证据**：**gsd** 量化到任务级的上下文预算（50% 硬预算、复杂度→占比表）+ fresh-context subagent；**trellis** 三层 hook 强制注入（"Specifications injected via hook, not recalled from memory"）；**web-dev-skills** protocols 单一真相源对抗规则漂移；**consensus-rnd** 同轮互盲 + controller 只读 marker 不读 prose。
- **机器可强制**：hook 注入（trellis/maestro/helloagents）；命令内容延迟加载（maestro invariant 9）。

### C3 记录与追溯（Journal/Trace）
- **失败模式**：出了问题无法回答"当时为什么这么做"；证据与结论断链。
- **证据**：**gsd** SUMMARY.md deviations 逐条对应 commit hash；**maestro** decisions.ndjson 追加式决策日志 + goal_changelog before/after 快照；**consensus-rnd** 一切决策在 GitHub 留痕（solver artifact、reviewer verdict、AI sentinel）；**spec-kit** FR 编号使需求→任务→代码可追溯。
- **机器可强制**：追加式结构化日志由脚本写入；证据文件路径写进状态（maestro completion_evidence）。

### C4 安全护栏（Safety Guardrails）
- **失败模式**：agent 为"完成任务"执行破坏性操作（rm -rf、force push、DROP TABLE）；prose 注入劫持编排。
- **证据**：**gsd** 禁破坏性 git 命令清单 + 分支 deny-list + 包安装人工门禁；**gstack** `/careful`（危险命令告警+白名单）`/freeze`（限制编辑目录）；**helloagents** guard.mjs 挂 PreToolUse 三层拦截；**consensus-rnd** 全面 fail-closed + controller 不读 prose 防注入。反例警示：missions 反暂停狂奔与安全护栏存在天然张力；helloagents No-Refusal 条款是负资产。
- **机器可强制**：PreToolUse hook 拦截（唯一真正可靠的形态）；缺配置 fail-closed。

### C5 完成权剥夺（Completion Gate）★新增独立维度
- **失败模式**：LLM 自证完成——没跑验证就说 Done（虚假完成），或在阶段节点自作主张停下（过早停止）。两者同根：**终止权在谁手里**。
- **裁决：独立成维度，不并入 S5 验证或 C7 机器下沉。** 理由见 §3 末行。
- **证据**：**maestro** invariant 6：`completion_confirmed=true` 只能由 CLI `maestro ralph complete` 写入；**helloagents** Stop/SubagentStop hook 上的 turn-stop-gate/ralph-loop，"无落盘 JSON 回执不算完成"是机器判定；**missions** 反暂停护栏（"非终态 turn 必须以工具调用结尾"、停止三问断言、continue_now 强制续跑）；**consensus-rnd** 完成认定只看退出码+sentinel+可解析 envelope，"日志文本绝不参与完成检测"；prompt 级鼻祖：**superpowers** verification-before-completion Iron Law。
- **机器可强制**：完成态字段只允许受控 CLI 写；Stop hook 门禁；sentinel/退出码判定。

### C6 跨host可移植（Portability）
- **失败模式**：方法论与单一 host 深绑，换工具即归零；子 agent 拿不到 host 私有机制加载的规则。
- **证据**：**superpowers** "Skills name actions, not tools" + 10 host 薄适配层 + 正式移植规范；**spec-kit** 单模板源占位符编译到 30+ host；**trellis** `.trellis/` 单一真源生成 16 平台产物 + 能力降级策略（无 subagent 的 host 走 inline 模式）；反面：gsd/maestro 深绑 Claude Code，多 host 只是安装器皮。
- **机器可强制**：构建期编译/投影（非运行时依赖）；版本同步自动化（superpowers `.version-bump.json`）。

### C7 机器下沉（Machine Offload / Runtime-Scripts）
- **失败模式**：把 FSM/校验逻辑写进 markdown 靠 LLM 自律（轻重光谱线索 4：这正是"又重又不可靠"的根源），弱模型下执行走样。
- **证据**：**consensus-rnd** 极致——上百个 pytest 把 prompt 的 marker/禁令/真值表锁成回归测试；**gstack** Bun 编译浏览器守护进程，验证是机器事实非模型口述；**helloagents** 73 个 .mjs 承担门禁/守卫/状态；**spec-kit** 分支/编号/前置校验全下沉 shell 脚本输出 JSON。反面：**missions** 零脚本全 prompt（状态漂移无兜底）、**maestro** 11 状态 FSM 写在 markdown 里靠模型自律。
- **机器可强制**：本维度即"可强制性"本身，是成熟度分水岭（线索 5）。

### C8 无人值守与多任务并行（Autonomy & Concurrency）★研究后追加（第二轮）
- **失败模式**：两种形态——①无人值守：中途人工门禁变死锁（agent 半夜停下问"是否继续"）、无人监督下虚假完成/裸奔破坏无人抓；②多任务并行：无认领锁则重复派发、无隔离则互相踩踏、沉淀物合并冲突。
- **证据**：**consensus-rnd** 全套协调基建（Spawn Claim 原子锁 #490、ActiveControllerLease、隔离 worktree、CODEX_FLOOR 并发配置、并发监控 pytest）；**gsd** auto 模式的分级降级范本（能自动的自动，唯 `gate="blocking-human"` 不可跳过）+ wave 零文件交集并行；**maestro** auto_confirm + ralph loop + Wave/Team/Swarm 四协作模式；**missions** 反暂停护栏是单流无人值守的 prompt 级天花板，但 CSV 单状态源结构性单流。反面：**spec-kit** implement 遇未完成 checklist 暂停问人、**superpowers** 流程灵魂是人机乒乓、**trellis** 0.5.0 主动移除 /parallel（清醒 scoping 但框架内无支持）。
- **与其他维度的关系**：无人值守是 C4/C5/S5 的需求放大器（人不在场，机器强制从加分项变生死线）；它与陪伴型美德（多问、常确认）天然对立，好的设计是**模式开关/分级降级**而非二选一。注意区分：任务内并行（fan-out 子 agent，多数框架已有）不算本维度，本维度只看**跨任务**。
- **机器可强制**：认领原子锁；门禁分级降级配置；隔离 worktree 边界断言。

---

## 三、用户候选概念逐条处理表

| 用户候选 | 裁决 | 理由与证据 |
|---|---|---|
| 步骤①澄清意图 | ✅ 保留（S1） | 5+ 框架有独立实现（spec-kit/superpowers/gstack/trellis/maestro），边界清晰 |
| 步骤②治理上下文 | 🔀 **移入横切**（C2） | 它不是时序中的一"步"：gsd 的预算贯穿全程、trellis 的 hook 注入每轮都发生、consensus-rnd 的互盲作用于审查阶段——所有证据都表明它与每个步骤同时发生，符合横切定义而非流水线节点 |
| 步骤③写清规格 | ✅ 保留（S2） | spec-kit 整个存在理由；missions 证明轻量场景规格可退化为 CSV 行但不可缺 |
| 步骤④小步切片 | ✅ 保留（S3） | 补充 maestro 的反过度切分证据：切片有上下界，不是越碎越好 |
| 步骤⑤实现功能 | ✅ 保留（S4） | 重点从"写码"改述为"带偏差协议的受控执行"（gsd Rule1-4、maestro re-grounding），因为 gstack 证明裸实现也能被前后门禁夹住，本步的增量价值在执行纪律 |
| 步骤⑥验证证据 | ✅ 保留（S5） | 10/10 框架均有投入，是共识度最高的一步 |
| 步骤⑦独立审查 | ✅ 保留（S6） | 强调"独立"的操作定义 = fresh context / 跨模型 / 互盲三种实现（superpowers/gstack/consensus-rnd） |
| 步骤⑧可控发布 | ✅ 保留（S7），但标注**生态空白** | 仅 gstack 做全、consensus-rnd 做半（无回滚）；保留它正是为了在对比表中暴露空白 |
| 步骤⑨复盘沉淀 | ✅ 保留（S8） | 闭环判据收紧为"回写 + 自动注入"双环（仅 trellis/maestro 达标） |
| 横切 Task State | ✅ 保留（C1），中文名"任务状态" | missions/web-dev-skills 提供了两种最干净的实现范式 |
| 横切 Journal/Trace | ✅ 保留（C3），中文名"记录与追溯" | 与 C1 区分：C1 答"现在在哪"，C3 答"当时为什么" |
| 横切 Safety Guardrails | ✅ 保留（C4），中文名"安全护栏" | 补充发现：与反暂停/No-Refusal 设计存在张力，评估框架时须两者对照看 |
| 横切 Tool Compatibility | ✅ 保留（C6），重命名"跨host可移植" | "Compatibility"易误解为工具调用兼容；证据显示真正的分水岭是"构建期投影 vs 运行时深绑" |
| 横切 Runtime/Scripts | ✅ 保留（C7），重命名"机器下沉" | 原名描述手段，新名描述动作；它同时是成熟度标尺（§4）的底层依据 |
| （新增）完成权剥夺 | ➕ **新增独立维度**（C5） | ①不并入 S5：验证是"产出证据"的动作，完成权是"谁有权盖章"的权限设计——missions 零机器验证却有最强反暂停护栏，证明二者独立变化；②不并入 C7：机器下沉是通用手段，完成权剥夺是特定失败模式（虚假完成+过早停止）的针对性对策，且存在纯 prompt 实现（missions 三问断言、superpowers Iron Law），有独立的 L1→L3 成熟度阶梯；③证据密度够：4 个框架（maestro/helloagents/missions/consensus-rnd）用 4 种不同机制实现同一原则，已构成独立模式 |

---

## 四、成熟度分级（供步骤 3 对比表评分）

> 依据线索 4/5：重普遍来自把 FSM 写进 markdown 靠自律；"机器强制 vs prompt 自律"是分水岭。每个维度/步骤按同一口径独立打分。

| 级别 | 名称 | 判据 | 典型证据 |
|---|---|---|---|
| **L0** | 缺失 | 未覆盖该能力，或仅一句口号 | 多数框架的"可控发布" |
| **L1** | Prompt 自律 | 规则写在 markdown/prompt 里，执行靠模型遵循；违规无机器后果 | missions 全框架；superpowers Iron Law；maestro 的 FSM 文本 |
| **L2** | 状态落盘 | 关键状态/证据/决策持久化为结构化文件，可校验、可恢复、可审计；但写入者仍是 LLM | spec-kit tasks.md `[X]`；web-dev-skills .state.json；gsd SUMMARY.md |
| **L3** | 机器强制 | 违规在机制层不可能：hook 拦截、CLI 独占写权、fail-closed、真值表门禁、契约测试锁 prompt | maestro invariant 6；helloagents Stop hook；trellis 三 hook 注入；consensus-rnd pytest 锁 prompt + fail-closed |

**评分口径（步骤 3 对比表用）**：
- 每格填 `L0/L1/L2/L3`，可加半级（如 L2+ 表示部分环节已 L3）。
- 判级看**最弱链**：某能力若关键路径仍靠 prompt 自律，即使有落盘文件也只算 L1+/L2-。
- L3 不等于"好"：需与臃肿度合看（consensus-rnd 全 L3 但接入成本极高）；轻量场景 L2 可能是最优取舍（missions 的 CSV）。
- 特别标注两个生态空白维度：S7 可控发布、S8 复盘沉淀——对比表中预期大面积 L0/L1，这本身是文章结论素材。

---

## 五、框架一页图

```
主线（8 步）：
  S1 澄清意图 → S2 写清规格 → S3 小步切片 → S4 实现功能
  → S5 验证证据 → S6 独立审查 → S7 可控发布 → S8 复盘沉淀
                                   ↑生态空白      ↑仅 2 家闭环

横切（8 项，贯穿全程）：
  C1 任务状态   C2 上下文治理   C3 记录与追溯   C4 安全护栏
  C5 完成权剥夺（新增）   C6 跨host可移植   C7 机器下沉
  C8 无人值守与多任务并行（追加）

成熟度：L0 缺失 → L1 Prompt 自律 → L2 状态落盘 → L3 机器强制
```
