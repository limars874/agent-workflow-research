# 好的 AI Agent 编码 Workflow 长什么样：8 步 × 7 横切 × 一条分水岭

> 基于对 superpowers、spec-kit、GSD、gstack、Trellis、web-dev-skills、consensus-rnd、maestro-flow、Missions、helloagents、mattpocock-skills 十一个开源 workflow/skill 框架的逐仓库深读，回答一个问题：**一个好的 agent 编码 workflow 应该具备哪些能力和步骤？**

---

## 一、引子：agent 不是不会写代码，是会撒谎、会迷路、会遗忘

如果你用 Claude Code 或 Codex 干过一个下午以上的活，大概率遇到过下面这些场景。它们不是模型能力问题，而是**流程问题**——而这正是 workflow 框架存在的理由。

**虚假完成（false done）**。agent 没跑测试，或者跑了没看输出，然后信誓旦旦地说 "All tests pass, the feature is complete"。superpowers 为此写下了它的 Iron Law："Evidence before claims, always"，甚至在 prompt 里预判并逐条驳斥模型可能找的偷懒借口。但请注意：这是 prompt 层的道德劝诫——maestro-flow 走得更远，它的 invariant 6 规定 `completion_confirmed=true` 这个字段**只能由 CLI 命令 `maestro ralph complete` 写入**，LLM 在机制层就没有自证完成的权力。同一个失败模式，两种截然不同的对抗强度。

**Context rot**。上下文窗口填到后半程，agent 的输出质量肉眼可见地降解：忘记早先的约定、重复已完成的工作、开始胡改。GSD 整个框架就是为对抗它而生——把上下文预算量化到任务级（50% 硬预算、复杂度到占比的映射表），每个任务派全新上下文的 subagent 执行。

**静默缩范围（silent scope reduction）**。遇到难点，agent 不声不响地把 "支持并发写入" 降级成 "TODO: 并发场景后续处理"，然后宣布完成。GSD 的 executor 偏差四规则明文写着 "No silent scope reduction"，架构级变更必须问人；maestro 每 3 个执行步插一道 re-grounding 门，检测到高置信漂移直接熔断（A_REGROUND_HALT，绕过 auto_confirm）。

**Mock 糊弄**。用 stub、dry-run、直接 import 内部模块绕过端到端路径，伪装真实验收。Missions 为此设计了 "管线 vs 模块" 判定：端到端验收禁止 import 内部模块绕过，每个工具有最低证据门槛。

**教训蒸发**。这次会话踩的坑、调通的姿势、发现的仓库暗礁，全留在聊天记录里，下次会话从零开始再踩一遍。十一个框架里只有两家把这个环节做成了闭环（Trellis 和 maestro，后面细说）。

把这些失败模式排成一条时间线——从拿到需求到上线后——你会发现它们天然对应一条流水线的各个环节。这就是下面这个能力框架的由来：**它不是从各框架 README 归纳的功能清单，而是从失败模式反推的对策清单。**

---

## 二、主流程：8 步流水线

```
S1 澄清意图 → S2 写清规格 → S3 小步切片 → S4 实现功能
→ S5 验证证据 → S6 独立审查 → S7 可控发布 → S8 复盘沉淀
                              ↑生态空白        ↑仅 2 家闭环
```

每一步下面按同一结构展开：它对抗什么失败模式、谁做得最好（具体到机制名）、哪些部分可以机器强制。

### S1 澄清意图（Clarify）

**失败模式**：拿到模糊需求直接开写；模型为跳过提问找 "合理化借口"（superpowers 明文对抗的第一失败模式）。

**最佳实践**：spec-kit 的 `/clarify` 是这一步的天花板——9 类歧义分类学、按 Impact × Uncertainty 排序、最多 5 问且一次一问、每个答案**原子写回 spec 文件**（而不是留在对话里）。superpowers 的 brainstorming 补上另一半：一次一问、给出 2-3 个方案权衡，且 "用户批准设计前禁止任何实现动作" 是硬门禁。Trellis 的 Evidence Rule 值得单独一提：**能查代码库回答的问题，绝不拿去烦用户**——澄清的成本应该花在真正只有人知道的信息上。

**机器可强制的部分**：提问质量无法机验，这一步天然弱机器化。但**门禁**可以强制：批准信号未落盘则禁止进入实现（Trellis 的 task-creation consent、helloagents 的 "用户确认计划前禁止写实现代码"）。

### S2 写清规格（Spec）

**失败模式**：意图只存在于对话历史里，session 一断即失真；需求没有编号，无法追溯 "这行代码对应哪条需求"。

**最佳实践**：spec-kit 的整个存在理由。FR-###/SC-### 编号让需求→任务→代码可追溯；requirements checklist 自校验限 3 轮；最精彩的是 `/converge`——实现完成后**反向对照** spec，把差异分为 missing/partial/contradicts/unrequested 四类。注意最后一类：unrequested，专抓 agent 手痒多写的东西。轻量端的反例证明规格可以退化但不能缺席：Missions 把规格压缩成 CSV 行，但 acceptance_criteria 强制 "WHEN X THEN Y; ref: file:line" 的机器可验格式。

**机器可强制的部分**：格式校验（Missions 的 19 列 schema、GSD 的 XML 四要素缺项拒写）；需求-任务覆盖矩阵（spec-kit 的 analyze）。

### S3 小步切片（Slice）

**失败模式**：一次改一大片，无法独立验证/审查/回滚；长任务中途遭遇 context rot 质量崩塌。

**最佳实践**：superpowers 给出了最好的切分**标准**——不是按代码量，而是按 "reviewer 能否独立批准或拒绝这一片"，粒度到 2-5 分钟一个任务。GSD 补充工程手感："vertical slices over horizontal layers"（竖切功能而非横切分层），每计划 2-3 任务，按 wave 依赖并行。但切片不是越碎越好——maestro 的 plan.md 明文反过度切分（"one feature = one task"）。**切片有上下界**，这是多数教程不会告诉你的。

**机器可强制的部分**：web-dev-skills 是列冠军——phase/slot/item 三层拆分、DAG 无环硬校验、循环依赖或缺字段**直接拒写**；GSD 用 ">5 文件即拆" 的阈值触发拆分。

### S4 实现功能（Implement）

**失败模式**：执行偏离计划无人察觉；遇阻静默降级；长实现污染主上下文。

这一步的增量价值不在 "写码"——gstack 证明了裸实现也能被前后门禁夹住——而在**带偏差协议的受控执行**。GSD 的偏差四规则是范本：Rule 1-3 分级自动修复、Rule 4（架构变更）必须问人、3 次尝试上限、全部记入 SUMMARY。helloagents 有个精巧的小规则："受阻前必须尝试 2 种实质不同的替代方案"——既防轻易放弃，又防无限死磕。superpowers 的 subagent-driven 模式则解决污染问题：每任务派全新 subagent，只发最小 task-brief。

**机器可强制的部分**：worktree/分支边界断言（GSD 对 main/master 的 deny-list）；漂移熔断（maestro 的 A_REGROUND_HALT）；consensus-rnd 的隔离 worktree + Spawn Claim 原子锁防重复派发。

### S5 验证证据（Verify）

**失败模式**："Should work" 式口头宣称；用 mock/stub 伪装真实完成。

这是**所有框架全部有投入、共识度最高的一步**，但成熟度差距最大。prompt 端的极致是 superpowers 的 Gate Function（"跑命令→读完整输出和 exit code→才能声明"）和 maestro 的 verify.md 禁语列表 + IDENTIFY→RUN→READ→VERIFY 单消息证据链。机器端的极致是两个 L3：**gstack** 用 Bun 编译的持久 Chromium 守护进程做真实浏览器点测截图——验证结果是机器事实，不是模型口述；**consensus-rnd** 更狠：完成认定只看退出码 + sentinel + 可解析 envelope，**"日志文本绝不参与完成检测"**——因为日志文本是 LLM 能伪造的，退出码不是。

**机器可强制的部分**：这是最应该机器化的一步。证据文件落盘才算数（helloagents："无 qa-review.json 不得宣称收口"）；GSD 强制每任务带 <60 秒的自动化 `<verify>` 命令，"Manual-only checks are forbidden"。

### S6 独立审查（Review）

**失败模式**：自己审自己——同一上下文、同一偏见，看不见自己的盲点。

"独立" 有三种操作定义，对应三个框架：**fresh context**（superpowers：每任务全新 reviewer subagent + fix 循环，最后由最强模型整分支终审）、**跨模型**（gstack：`/review` + `/codex` 跨厂商双模型对抗审查，Codex 发现 P1 直接阻断 ship；且 finding 必须引用具体代码行，引不出就降置信）、**互盲**（consensus-rnd：三个有偏 reviewer 同轮互相看不见彼此，最终由一张机械真值表裁决合并：`reject==0 && approve>=1 && head 一致`——这张真值表被 pytest 锁成回归测试）。

评分矩阵里这一维度**两极分化**最严重：要么 L3 级对抗设计，要么退化成同 agent 自审的 L1（spec-kit、helloagents、Trellis 都栽在这里），中间形态很少。

**机器可强制的部分**：合并真值表；review head == PR head 校验（防审完又改）；P1 门禁阻断。

### S7 可控发布（Release）——生态最大空白

**失败模式**：几乎所有框架止于 commit/PR。部署、灰度、健康检查、回滚？没有。

唯一做全链路的是 gstack：`/ship`（测试门禁→PR）→ `/land-and-deploy`（合并+部署+健康检查）→ `/canary`（上线后监控）。consensus-rnd 的 Release Publisher 做了半程：exact-SHA required-checks 全绿才发布——但文档明言 "不回滚"。**回滚/灰度预案在十个框架里是零覆盖**（gstack 的 `/canary` 只是监控，不是回滚）。

有个耐人寻味的悖论：发布恰恰是天然最可机器化的环节（CI required checks、健康检查、金丝雀指标都是现成的机器事实），却被 "prompt 框架" 们集体外包给了 CI。外包本身多数是清醒的 scoping（把不可逆操作留在人手里）——但**变更的可回滚性、以及生产反馈回流到 agent 世界的通道，不是 CI 自带的**。这是本框架保留 S7 的理由，具体分析见第六节。

### S8 复盘沉淀（Retro）——仅两家闭环

**失败模式**：教训留在聊天记录里蒸发，同一个坑每个 session 重踩一遍。

判闭环的标准要收紧为**双环**：不只 "回写"（把教训写进文件），还要 "自动注入"（下次执行时机器把它塞回上下文，而不是指望模型想起来去读）。达标的只有两家：

- **Trellis**：spec-sync 是 commit 的**前置条件**——不沉淀就不许提交；trellis-update-spec 是七段式可执行契约模板；沉淀物通过 hook 注入下次会话（"Specifications injected via hook, not recalled from memory"）。
- **maestro**：retrospective 把洞见三路由（specs / issues.jsonl / learnings.md），再由 17 个 hooks 自动注入下次执行。它的口号概括了整个环节："Workflows generate knowledge, knowledge improves future workflows"。

次一级的是 gstack 的 `/learn`（learnings.jsonl 被其他 skill 前置查询，但查询动作靠 prompt）和 helloagents 的 hello-reflect（有明确触发条件：2+ 验证循环 / 调试超 3 次 / 换方案）。spec-kit 和 Missions 这一格是 L0——完全没有。

---

## 三、横切能力：8 项贯穿全程的基础设施

步骤是时序，横切能力是每一步都在起作用的底座。

### C1 任务状态（Task State）——回答 "现在在哪"

断线或 compaction 后不知身在何处，是长任务最常见的死法。两个最干净的实现范式：Missions 用 CSV 做唯一状态源，配 mission-recovery 修复不一致状态；web-dev-skills 的 `.state.json` **刻意零时间戳**——纯 status 枚举 + 文件存在性 + DAG，任何时刻都能无状态重建，这个设计值得单独抄走。最高分是 consensus-rnd：状态全在 GitHub labels/comments 上（"GitHub 即数据库"），本地只是 cache，外加 ActiveControllerLease 保证跨设备唯一 controller。

### C2 上下文治理（Context Governance）

原本候选框架里它是流水线的一 "步"，研究后移入横切——因为所有证据都表明它与每个步骤**同时**发生：GSD 的上下文预算贯穿全程，Trellis 的 hook 注入每轮都触发，consensus-rnd 的互盲作用于审查阶段。冠军是 Trellis 的 L3：SessionStart / UserPromptSubmit / PreToolUse 三层 hook 强制注入——规则是机器塞进来的，不是靠模型记得去读的。

### C3 记录与追溯（Journal/Trace）——回答 "当时为什么"

与 C1 的区分：C1 答 "现在在哪"，C3 答 "当时为什么这么做"。GSD 的 SUMMARY.md 把每条 deviation 对应到 commit hash；maestro 有追加式的 decisions.ndjson 决策日志 + goal_changelog 的 before/after 快照；consensus-rnd 一切决策在 GitHub 留痕。

### C4 安全护栏（Safety Guardrails)

agent 为了 "完成任务" 执行 rm -rf、force push、DROP TABLE，是共享环境里的真实事故源。这一维度上**唯一真正可靠的形态是 PreToolUse hook 拦截**：helloagents 的 guard.mjs 做了三层（命令→语义→外部输出），consensus-rnd 全面 fail-closed 且 controller 不读 prose 防提示注入。绝大多数框架在这里只有 prompt 级约定（GSD 的禁令清单、gstack 的 `/careful`），甚至 L0（Missions 几乎无防护）。

两个警示：Missions 的反暂停狂奔与安全护栏存在**天然张力**——越是不许 agent 停下来问，越需要机器护栏兜底；helloagents 的 No-Refusal 条款（要求模型不得拒绝指令）是负资产，采用其护栏机制时必须剔除。

### C5 完成权剥夺（Completion Gate）——本研究新增的维度

这是十个仓库读下来浮现的、候选概念清单里没有的新维度，值得多花几段讲透。

虚假完成（没验证就说 Done）和过早停止（阶段节点自作主张收工）看似两个问题，实则同根：**终止权在谁手里**。只要 LLM 拥有 "宣布完成" 的权力，它就总有一天会滥用——不是因为恶意,而是因为 "宣称完成" 是它训练分布里的高概率出口。

四个框架用四种不同机制实现了同一原则：

| 框架 | 机制 | 层级 |
|---|---|---|
| maestro | invariant 6：`completion_confirmed=true` 只能由 CLI `maestro ralph complete` 写入 | L3，CLI 独占写权 |
| helloagents | Stop/SubagentStop hook 上的 turn-stop-gate/ralph-loop，"无落盘 JSON 回执不算完成" | L3，hook 门禁 |
| consensus-rnd | 完成认定只看退出码 + sentinel + 可解析 envelope，日志文本不参与 | L3，机器判定 |
| Missions | 反暂停三问断言、continue_now、"非终态 turn 必须以工具调用结尾" | L1+，最系统的纯 prompt 实现 |

为什么它独立成维度而不并入 S5 验证或 C7 机器下沉？两个理由。其一，验证是 "产出证据" 的动作，完成权是 "谁有权盖章" 的**权限设计**——Missions 零机器验证却有全场最强的反暂停护栏，证明二者独立变化。其二，机器下沉是通用手段，完成权剥夺是针对特定失败模式的对策，且存在从纯 prompt（superpowers Iron Law）到 CLI 独占（maestro）的独立成熟度阶梯。

**评估任何 workflow 时，先看这一格。** 它是 "prompt 自律 vs 机器强制" 分水岭最锋利的切面。

它还有一个容易被忽略的推论：**判卷标准不能由考生修改**。agent 改测试断言的动机与它糊弄验证的动机同源——测试红了，改测试比改代码 "便宜"。完成权剥夺不只管 "谁能写 done"，也管 "谁能改 done 的定义"。

### C6 跨 host 可移植（Portability）

方法论与单一 host 深绑，换工具即归零。分水岭不在 "支持几个平台"，而在**构建期投影 vs 运行时深绑**：superpowers（"Skills name actions, not tools" + 10 host 薄适配层 + 正式移植规范）和 spec-kit（单模板源占位符编译到 30+ host）是 L3；GSD 和 maestro 的 "多 host 支持" 只是安装器皮，语义深绑 Claude Code。Trellis 的能力降级策略值得一提：host 没有 subagent 就走 inline 模式——优雅降级好过硬性要求。

### C7 机器下沉（Machine Offload）

把 FSM 和校验逻辑写进 markdown 靠 LLM 自律，是十个框架里 "又重又不可靠" 的共同根源——prompt 越写越长来堵漏洞，弱模型下执行照样走样。极致的正面是 consensus-rnd：**上百个 pytest 把 prompt 里的 marker、禁令、真值表锁成回归测试**——prompt 改坏了，CI 会红。反面的两个典型：Missions 14 个 markdown 零脚本（状态漂移无兜底）；maestro 空有 8 万行平台，核心的 11 状态 FSM 却写在 markdown 里靠模型自律，这一个决定拖累了它矩阵里的多格半级。

### 插一节：机器强制到底该用什么实现？

说了这么多 "机器强制"，值得停下来把实现手段本身盘一遍——因为选错了层，L3 也是纸糊的。十一个框架里实际出现过五种强制形态：

| 形态 | 谁在用 | 能力边界 |
|---|---|---|
| **Agent host hook**（PreToolUse/Stop/SessionStart） | helloagents（.mjs）、trellis、maestro（17 hooks）、mattpocock（shell） | 唯一能做**事前拦截**的形态；代价是深绑 host |
| **CLI 独占写权** | maestro（`ralph complete`）、gsd（gsd-tools）、trellis（task.py） | host 无关；但只约束走正门的——状态终究是本地文件 |
| **Git 原生 hook**（pre-commit/pre-push） | 生态几乎无人用（被低估） | host 无关、天然存在、换 Codex/opencode 照样生效 |
| **服务端门禁**（分支保护 + required checks） | consensus-rnd（exact-SHA 全绿才动）、gstack | 规则在 GitHub 服务器上，**agent 的工具物理触达不了** |
| **契约测试锁 prompt** | consensus-rnd（上百 pytest） | 锁的不是 agent 行为，而是 "规则本身不被改坏" |

**第一条原则：强制力 = 该机制离 agent 写权限的距离。** hook 配置、git hook 脚本、CLI 管的状态文件，都在 agent 有写权限的半径内；分支保护和 required checks 在另一台机器的服务配置里——打个比方，本地 hook 是你在家门口贴的 "禁止入内"，服务端门禁是小区保安：agent 在你家里怎么折腾，都改变不了保安的放行规则。

**但要按真实威胁模型校准，不要极端化。** agent 绕护栏不是越狱式蓄意，而是目标导向的抄近路——它把门禁当成待解决的障碍。所以风险不取决于 "它能不能改"，而取决于**这条绕路在不在它的习惯路径上**：`git commit --no-verify` 是真实风险，因为训练数据里有大量人类工程师 "被 hook 挡了就加 --no-verify" 的合法先例，agent 被拦几次后试这招是常见行为；而主动改 `.git/hooks/` 或 settings.json 拆自己的镣铐是尾部事件，正常场景基本不发生。结论：**本地机制防 "粗心 + 抄近路"，覆盖 95% 以上的真实失败**——只需额外堵上 `--no-verify` 这条著名近路（PreToolUse 拦这个字符串，一行规则）；服务端门禁不是因为本地不可靠，而是按赌注分配：main 分支、发布、生产数据这类错一次就疼的地方才值得 "物理不可绕"。还有一个变量会移动这条线：无人值守时长——陪伴模式下绕路你当场看见，放跑一整夜则一次绕路后面跟着数小时无人纠正的狂奔，**C8 用得越重，关键门禁越该往服务端挪**（护栏等级跟着兜底能力走）。

**按拦截时机分三层组装**：

1. **事前**（防不可逆破坏）：只有 agent host hook 能做——危险命令拦截这件事别无选择；
2. **事中**（完成权/状态/提交质量）：CLI + git hook——host 无关，够用，接受 "防抄近路不防蓄意"；
3. **事后**（最终兜底）：分支保护 + required checks——不可绕、免费、零 host 耦合，`main` 禁直推 + 测试挂 required 两分钟配完，是不用任何框架就能白得的 L3。

**git pre-push 上还能放一个生态缺失的组件：跨模型二审**。前文说过独立审查两极分化、缺轻量中间形态——它的最小实现就是一个 pre-push hook：`git diff origin/main...HEAD` 连同 PRD 喂给另一家模型的 headless CLI（如 `codex exec`），令其找缺漏/糊弄/范围偏离，输出含 BLOCK 则 exit 1。这天然满足独立审查的两个定义（跨模型 + fresh context：审查方只看 diff 和 PRD，看不到实现者 "我已经很努力了" 的叙事）。工程注意点：挂 pre-push 别挂 pre-commit（AI 评估几十秒起步，会逼死小步提交；push 是天然批量节点）；异常放行 + 落盘告警，别 fail-closed（否则第三方 API 一抖你就没法 push）；AI 判定不确定，适合做 "抓缺漏的网" 而非 "必须绿的门"——确定性的门留给 CI；结论落盘 `reviews/<sha>.md` 以便追溯。

最后两条工程细节：**脚本语言不重要，判据是零依赖启动**（hook 每轮都跑，不能有 venv/npm install 前置；helloagents 选 .mjs 因为装 Claude Code 必有 node，shell 最稳）、**快**（PreToolUse 在关键路径上）、**退出码语义清晰**；以及 consensus-rnd 那条被普遍忽视的教训——**门禁脚本自己要有测试**，门禁代码改坏了没人知道，等于没门禁。三层全部加起来不到两百行配置和脚本，不依赖任何框架——这就是 "只抄机制" 路线在强制层的完整落地。

### C8 无人值守与多任务并行（Autonomy & Concurrency）——追加的维度

Agent 开发在使用形态上有一条真实的路线分岔：**陪伴型**（人全程参与讨论/验收）与**放跑型**（发任务让 agent 自己跑完）。放跑型对 workflow 的需求清单和陪伴型几乎是反的：中途零人工门禁（agent 半夜停下问 "是否继续" 等于任务死亡）、受阻自处理协议、以及多任务并发时的协调基建（认领锁防重复派发、隔离防踩踏、沉淀物合并）。同一条规则——"架构变更必须问人"——在陪伴模式是护栏，在放跑模式是死锁。

这一维度上生态分成三个阵营。**为放跑而生**：consensus-rnd 是独一档（Spawn Claim 原子锁、ActiveControllerLease、隔离 worktree、并发监控 pytest 全套协调基建）；Missions 的反暂停护栏是单流无人值守的 prompt 级天花板，但 CSV 单状态源结构性单流且近乎零安全护栏。**有放跑挡位**：gsd 的 auto 模式是分级降级的范本——能自动的自动、记入 SUMMARY 事后审，唯 `gate="blocking-human"`（如装包）不可跳过；maestro 的 auto_confirm + ralph loop 类似。**结构性阻碍**：superpowers（一次一问的人机乒乓是流程灵魂）、spec-kit（implement 遇未完成 checklist 暂停问人）、trellis（0.5.0 主动移除 `/parallel`，consent 门禁为陪伴设计）。

两个推论。其一，无人值守是 C4/C5/S5 的**需求放大器**：人不在场，机器强制从加分项变成生死线——这也是第四节 "选级看兜底能力" 规则的极端情形（兜底能力为零）。其二，并行不是快的同义词：并行加速的上限是人的串行参与部分（Amdahl 定律），而语义冲突成本随并行度超线性增长。**并行划算的条件苛刻且三者缺一不可**：任务间零文件交集、规格已 100% 写死、机器可端到端验收。满足的（批量补测试、迁移 API、修一类 lint）分流出去放跑；不满足的（核心 feature）串行陪伴不是妥协，是正确答案。

---

## 四、成熟度阶梯：L0 → L3，以及为什么 L3 不等于好

| 级别 | 名称 | 判据 |
|---|---|---|
| **L0** | 缺失 | 未覆盖，或仅一句口号 |
| **L1** | Prompt 自律 | 规则写在 markdown 里，靠模型遵循；违规无机器后果 |
| **L2** | 状态落盘 | 关键状态/证据持久化为结构化文件，可校验可恢复；但写入者仍是 LLM |
| **L3** | 机器强制 | 违规在机制层不可能：hook 拦截、CLI 独占写权、fail-closed、真值表门禁、契约测试锁 prompt |

三条使用规则：

1. **判级看最弱链**。某能力若关键路径仍靠 prompt 自律，即使有落盘文件也降级。maestro 的 S4 就是例子：有 re-grounding 门和熔断，但 FSM 本体在 markdown 里，所以只有 L2。
2. **L3 ≠ 好**。consensus-rnd 七个维度 L3，但代价是 daemon 群 + 45 个配置变量 + 上百测试的极高接入成本。它是 "全自治仓库研发实验"，不是给你日常写 feature 用的。
3. **轻量场景 L2 可能是最优取舍**。Missions 用 14 个 markdown、一张 CSV，在 C1/C5 上打出超越体量的分数。如果你的任务是 "已有清晰需求的长任务断点续跑"，它比任何重型框架都合适。
4. **选级看兜底能力**。前三条隐含一个假设：人是最后一道审查网——agent 跑偏了，懂代码的你扫一眼 diff 就能抓到，人力可以补齐机器没强制的部分。但当你在陌生技术栈上开发（比如 web 工程师做原生 app），你只能在两个黑盒端口参与：前端的需求讨论和末端的手动测试，中间整段 S3→S6 你既看不懂 diff 也判断不了架构决策。此时**你的评审能力覆盖不到的环节，就是必须机器强制的环节**。L 级需求不是框架的属性，是 "框架 + 使用者 + 技术栈" 这个组合的属性——同一个人换个技术栈，需要的成熟度等级就变了。

### 还有一维没打分：流程税（Process Tax）

同一个任务，从下发到完工要花多少时间和 token，是一个真实存在但本文刻意没纳入矩阵的维度——因为它是唯一**必须实测才能打分**的：其他维度看仓库静读即可判级（机制存在与否是事实），而 time/token 是运行时属性，与任务类型、模型、仓库大小强相关，没有 benchmark 就填数字等于拍脑袋。但不实测也有三件事可以说清楚：

1. **固定开销可以静态估算，且是结构性下限。** 每个框架对单个 task 的最小工序数写死在流程里：superpowers 一个 2-5 分钟的任务背后至少 implementer + spec reviewer + code reviewer 三个 subagent；GSD 的编排/代码 token 比达 4:1；spec-kit 每个 feature 强制 5+ 文档。这不是 "可能慢"——改一行文案也得付这么多。

2. **多数框架缺一个分流阀（triage）。** 它们默认所有任务都值得走全流程，把 "这个任务配不配上仪式" 的判断外包给了用户 "要不要启用框架" 这个二元选择。这本身是设计缺陷：**好的 workflow 应该在 S1 入口就判定任务量级，小任务走短路径**，而不是让用户在全套仪式和裸奔之间二选一。十个框架里没有一家把任务分级路由做成显式机制。

3. **流程税和可靠性的关系不是线性的。** 重的两种来源在成本上表现完全不同：买能力的重（hook、验证守护进程）多是一次性安装成本，运行时几乎不耗 token；伪复杂度的重（FSM 写进长 prompt 靠自律）**每个 task 都重复付费**。最该砍的恰恰是后者，而后者才是 token 大头——所以 C7 机器下沉同时是个降本手段：下沉到脚本的逻辑不再消耗上下文。

若要量化，实测思路并不复杂：取一组分级任务（改文案 / 加一个字段 / 跨模块重构），各框架跑一遍，记录 token 消耗与 wall-clock，再对照各自的 L 级看 "每一级可靠性花了多少税"。本文未做，留给有条件的人。

---

## 五、10 框架点评

先上精简矩阵（完整 10×16 L 级矩阵见 `03-对比表.md`）：

| 框架 | 剖面形状 | 最亮机制 | 最大短板 | 臃肿/耦合 | 适用场景 |
|---|---|---|---|---|---|
| superpowers | 均衡中型 + 可移植单尖峰 | 对抗性 prompt + 10 host 移植规范 | 后段塌陷（发布/复盘/护栏/完成权全 L1） | 本体轻、运行时 token 重；耦合低 | 任何 host 上的默认工程纪律底座 |
| spec-kit | 前段双峰，S5 后断崖 | /clarify 歧义分类学 + /converge 反向对照 | 验证 L1、自审 L1、复盘 L0 | 重（每 feature 5+ 文档）；耦合极低 | 中大型 feature 的规格驱动，跨工具团队 |
| GSD | 中段厚实的均衡重型 | 任务级上下文预算 + 偏差四规则 | 无 L3 尖峰，机器下沉不足 | 极重（4:1 编排 token）；CC 深绑 | 大型 greenfield 全程托管（选社区 fork） |
| gstack | 后段尖峰 | Chromium 守护进程验证 + 跨模型对抗审查 | S3/S4 反而 L1，大特性靠人肉切 | 重（40+ skill + 守护进程） | Web/iOS 全栈项目，需真实验证与发布链 |
| Trellis | 记忆型 | spec-sync commit 前置 + 三层 hook 注入 | 审查 L1+、止于 commit | 重；接口跨平台但灵魂在 CC；AGPL | 多人长期仓库的规范沉淀 |
| web-dev-skills | 窄而深 | DAG 硬校验 + 无时间戳 .state.json | 绑死 Rust+React 技术栈 | 偏重，栈耦合深 | 仅对口技术栈团队全盘采纳 |
| consensus-rnd | 后段全 L3 重装 | 互盲审查 + pytest 锁 prompt + fail-closed | 无人形：S1/S3 全场最弱，无回滚 | 极重；GitHub+双模型深绑 | 全自治研发实验；子集可单点抄 |
| maestro-flow | C5 单点 L3 + 全面 L2 平台 | CLI 独占完成态（invariant 6）+ 知识图谱回注 | FSM 在 markdown，安装易迁出难 | 极重（8 万行） | all-in 一套流程的重度长期项目 |
| Missions | 单点尖峰轻量 | 反暂停护栏 + CSV 状态源 + 验收合同 | 零脚本零护栏零复盘（三格 L0 级） | 极轻（14 个 markdown） | 清晰需求的长任务断点续跑，配前段框架 |
| helloagents | 门禁三峰（C4/C5/C7 全 L3） | guard.mjs 三层拦截 + Stop hook 完成门禁 | 自审 L1；No-Refusal 是合规负资产 | 重（73 脚本黑箱） | 个人重度使用者的交付门禁层 |
| mattpocock-skills | 全场最平的 L1-L2 面 + craft 纵深 | grilling 对齐工艺 + seam TDD/feedback-loop 调试方法论 | 机器强制≈零（3 个脚本）；执行编排薄；S7=L0 | 极轻（纯 markdown，单 skill 可拆用） | 会自己编排流程的资深工程师做人机结对 |

几句超出表格的话：

- **superpowers** 是 "prompt 工程的天花板、机器工程的地板"。它把工程纪律写成了最好的散文（预判偷懒借口逐条驳斥），但 Iron Law 后面跟的威胁是 "If you lie, you'll be replaced"——没有任何机器后果。适合做底座，不能做门禁。
- **spec-kit** 的哲学一句话："只管想清楚，不管做到位"。前段 S1/S2 无出其右，S5 之后断崖。单用它等于雇了个只写 PRD 的产品经理。
- **gstack** 的 "两头门禁夹裸实现" 直接写在分数里：S5/S6/S7 领先，S3/S4 却是 L1。它赌的是验证和审查够硬，实现过程可以放养——对小任务成立，大特性会在中间散架。
- **consensus-rnd** 是全场机器化最彻底的，也是最没有 "人" 的：几乎没有向人澄清意图的环节。它回答的问题不是 "怎么帮我写代码"，而是 "仓库能不能自己研发自己"。**一手实测（n=1）**：一个项目从 idea issue 到 merge 完成花了约 5 小时。慢是结构性的两因子相乘：工序多（solver 交卷→reviewer 互盲领取→verdict→真值表→merge，每步都过一遍总线）× 单步延迟高（GitHub 即数据库意味着每次状态流转都走轮询 + 受 API 速率限制）。这暴露了流程税的第二种形态——**通信税**，且它与工序数是乘法关系。架构上高频协调本可下沉本地、只留低频审计与门禁在 GitHub，但它上百个 pytest 契约全围绕 GitHub 语义编写——机器下沉锁死了正确性，也锁死了架构。
- **maestro** 是最矛盾的一个：发明了完成权剥夺这个最锋利的机制，却把自己的 FSM 留在 markdown 里靠自律——用 8 万行平台围着一个 prompt 自律的核心。
- **mattpocock-skills**（Matt Pocock 的个人 `.claude` 目录直出，~157k stars）是对本文评分口径最好的**压力测试**。它是刻意的反框架派——作者公开批评 GSD/BMAD/Spec-Kit "接管流程夺走控制权"——所以在 L 级矩阵上呈现全场最平的 L1-L2 面，无一尖峰。但它的偏科科目在矩阵度量不到的地方：grilling（一次一问 + **每问附推荐答案** + 能查代码绝不问用户）是生态最深的对齐工艺；tdd 的 seam 概念、反同义反复测试（期望值不得用与实现相同的方式重算）、diagnosing-bugs 的 "构造出反馈回路之前禁止读代码建理论"，是别家只有 "必须跑测试" 口号时它独有的 **"什么样的测试才算数" 的鉴别力**。它证明了 L 级衡量的是强制力不是判断力——两者正交，而它赌的是后者：对齐和验证的判断力做到位，前面省掉的返工比机器门禁挡下的更多。这个赌注只对一类人成立：会自己编排流程、能人肉充当门禁的资深工程师；弱模型或无人值守场景下它没有任何兜底（157k star 里有多少是 KOL 光环，读者自行打折）。
- **Trellis 补一条一手实测（n=1）**：陪伴型开发一个 Mac app 近一个月的重度使用，"建 task → 深度讨论 → 放手执行 → 人参与测试"，全程基本没有跑偏。不跑偏的根源大概率就是它唯一的 L3（三层 hook 强制注入——规则是机器每轮塞进上下文的，不是靠模型记得去读）；且这种用法恰好只踩它的强项区（S1/S2/C1/C2/S8），弱格 S6 由人工测试顶上、S7 止于 commit 对个人项目无碍——**按剖面用框架、不为弱项付费**的天然示范。一个月未被其 "重" 劝退，也佐证它的重集中在安装期/产物层，摊到每个 task 的流程税并不高。

---

## 六、生态四大空白：这是机会，不是遗憾

**空白一：发布不是空白，接缝才是。** 十一家中七家在 S7 只有 L1 或更低；唯一 L2+ 的 consensus-rnd 明言不回滚；gstack 的 `/canary` 只监控不回退。但公允地说，"止于 commit/PR" 多数是清醒的 scoping 而非疏忽：PR 之前 agent 搞砸了浪费的是你的时间，部署之后搞砸的是用户和钱——把不可逆操作留在人手里，本身就是最大号的护栏；何况生产发布那一侧早有成熟的机器化基建（CI/CD、feature flag、金丝雀平台），那个领域本来就是 L3 的，不需要 prompt 框架重新发明。

真正的空白在**两个系统之间的接缝**，有两处：

- **回程通道**。agent 交付的 PR 上线后被金丝雀指标劣化触发回滚——这个信息永远回不到 agent 的世界里。下个 session 它不知道自己上次的产出被回滚过；trellis 和 maestro 的双环闭的都是 dev 内部的环，S8 复盘沉淀的输入源里缺了生产这一环。
- **可回滚性作为交付标准**。agent 在 dev 阶段就该被验收 "这个变更可回滚吗"：migration 有没有 down、破坏性 schema 变更有没有过渡期、feature flag 有没有包住新路径。这是 dev 环节的事，却没有一家框架把它写进 `<verify>` 或验收标准。

所以机会的准确表述不是 "谁去做 agent 发布"，而是：**让 agent 交付 "可被安全部署和回滚的变更"，并让生产反馈（回滚事件、劣化指标）成为 S8 的输入源**。前者是一条验收规则，后者是一个 webhook 到 learnings 文件的管道——两者的实现成本都远低于它们的价值。

**空白二：复盘沉淀，仅两家双环闭合。** Trellis 和 maestro 之外，其余九家要么 L0/L1，要么只有 "回写" 没有 "自动注入"。这意味着市面上绝大多数 agent workflow 的用户，每个 session 都在为同样的仓库暗礁重复付费。而实现双环的技术门槛并不高：一个 commit 前置门禁 + 一个 SessionStart hook。

**空白三：没有一家为存量重构设计过流程。** 十个框架的流水线全部默认 "从无到有开发新功能"，但 agent 时代重构的失败率远超直觉，失败模式也很特定——**重构回归**：已完成并测试过的功能（尤其细节判断逻辑）在重构中静默蒸发。机理在于：人重构是 "变换" 现有代码，细节被搬运；agent 重构本质是按当前上下文里的理解**重新生成**——上下文装不下旧代码的全部细节，装不下的部分默默消失。任务越大蒸发越多，这是 context rot 在重构场景的特殊表现。更糟的是常规护栏会连带失效：架构级重构时测试代码往往也被一并重写，特征测试跟着蒸发——护栏和被护对象同时施工，网就没了。

三道防线（按成本排序）：

1. **重构前产出行为清单**：让 agent 通读旧代码，把所有行为（尤其 edge case）列成带编号 inventory 落盘，人过目补充，重构后逐条对照——spec-kit `/converge` 思路的手动版。
2. **特征测试写在最外层稳定边界**（CLI/API/UI 级的端到端断言）：内部架构怎么翻，外部行为断言不用动，测试自然不需要 "被重构"——这是让 "冻结测试" 变得现实的前提。目录级冻结（gstack `/freeze` 思路）只对这类测试可行；对单元级测试，"只许改调用方式不许改断言" 难以机器判定，退而求其次是测试变更走独立 commit、由人或第二个 AI 单独审。
3. **重构也走切片（S3）**：大重构一次做完等于一次性重新生成一切；切成小片，每片的旧行为都还装得进上下文。

一个 n=1 的有效补救实证：重构代码先不提交，把该 task 的 PRD 交给**另一个** AI 对照代码找缺漏——找回了不少蒸发的功能。这正是 spec-kit `/converge`（反向对照抓 missing）与 S6 fresh-context 审查的手动组合：干重构的 agent 上下文里装着 "我已完成" 的自我叙事，让它自查等于让作案者验尸；新上下文的 reviewer 没有这个偏见。

**空白四：多任务协调没有轻量选项。** C8 的评分光谱只有三档：consensus-rnd 的完整协调基建（代价是 GitHub 总线全家桶与通信税）、一堆框架的 "worktree 隔离 + 自求多福"、web-dev-skills/Missions 的显式单流。中间是空的——**没有一家提供 "轻量任务认领锁 + 状态隔离 + 沉淀物合并" 这种不带总线全家桶的多任务协调层**。consensus-rnd 证明了需求存在但解法太重；trellis 们把问题推给宿主，而宿主的原生 worktree 只解决文件隔离，不解决任务认领和沉淀合并。这个组件的合理形态可能只是几百行：一个基于文件锁的认领机制 + append-only 的沉淀 journal（每 worktree 一个文件，merge 天然不冲突）+ 合并后的规范编译步骤。

附带一个次级发现：**独立审查两极分化**——要么 consensus-rnd/gstack 级的对抗设计，要么退化为自审。市面缺一个轻量的、可单独引入的 "跨模型二审" 组件（gstack 的 `/codex` 是最接近的原型）。

---

## 七、组合建议：按任务选原型，按机制做拼装

评分矩阵的总结论：**"全能框架" 不存在——16 维中 L3 过半的只有 consensus-rnd 一家，而它以极高接入成本、无前段人机交互和每任务小时级的通信税为代价；其余十家 L3 都不超过 5 格。** 存在的是四种原型：前段规格型（spec-kit）、执行闭环型（Missions/GSD）、后段门禁型（gstack/consensus-rnd/helloagents）、记忆沉淀型（Trellis/maestro）。选型 = 按你的失败模式选原型，再跨原型拼装。

### 三套拼装方案

**方案 A：轻量长任务 —— spec-kit + Missions**
spec-kit 补 Missions 全缺的 S1/S2 前段，Missions 补 spec-kit 弱的 C1/C5 执行闭环。两者 host 耦合都低，拼出全流程 L2 面。Missions 作者的 codex-missions.md 已半官方推荐了类似组合。适合：需求明确、周期数天、单人。

*轻量变体*：前段换成 mattpocock 的 grilling → to-prd → to-issues 链——同样覆盖 S1→S3 但轻得多（三个可拆用 skill vs 每 feature 5+ 文档）。代价是丢掉 FR/SC 需求编号和 `/converge` 反向对照。分岔标准：小中型任务用轻变体；需要需求追溯、或有重构回归风险的大 feature 仍用 spec-kit（converge 的 missing 检测正是防功能蒸发的关键件）。

**方案 B：全栈产品交付 —— superpowers + gstack**
superpowers 补 gstack 的 S3/S4 实现纪律，gstack 补 superpowers 的 S5(L3)/S7/C4 后段。16 维中 13 维 ≥L2，是覆盖面最大的双拼。仍缺：S8 复盘、回滚。适合：Web/iOS 产品持续迭代。

**方案 C：多人长期仓库 —— Trellis + 外挂跨模型二审**
Trellis 提供 C2=L3 的上下文注入和 S8 的 spec-sync 沉淀，外挂 gstack 式的 `/codex` 跨模型审查补它 S6=L1+ 的洼地。适合：知识密度高、人员流动、跑一年以上的仓库（注意 AGPL）。

**通用增强层：mattpocock 的 craft 三件套可叠加到任意方案上。** grilling（对齐）、tdd（seam + 反同义反复）、diagnosing-bugs（feedback-loop-first）是单 skill 可拆、不接管流程的设计，与任何编排都不冲突——它们补的不是流程结构而是判断力，且注意它们**不能替换**方案中的结构件：mattpocock 与 superpowers 看似同类，但方案 B 里 superpowers 的角色是补 gstack 的 S4 执行纪律，而 S4 恰是 mattpocock 自己的弱项（implement 仅薄清单）。

### 不引全家桶，只抄机制

无论选哪套，以下单点机制都值得直接移植——它们互相独立、抄的成本远低于引入原框架：

| 机制 | 出处 | 解决什么 |
|---|---|---|
| CLI 独占写完成态 | maestro invariant 6 | 虚假完成，一劳永逸 |
| PreToolUse 三层拦截（剔除 No-Refusal） | helloagents guard.mjs | C4 护栏——全生态普遍洼地，且这是唯一可单独抽取的安全件 |
| pytest 锁 prompt 契约 | consensus-rnd | prompt 改坏无人知——把 marker/禁令/真值表变成回归测试 |
| 无时间戳 .state.json | web-dev-skills | 断点恢复——status 枚举 + 文件存在性 + DAG，任意时刻无状态重建 |
| /converge 反向对照 | spec-kit | 静默缩范围 + 手痒多写（unrequested 分类） |
| spec-sync 作为 commit 前置 | Trellis | 教训蒸发——不沉淀不许提交 |
| "日志文本不参与完成检测" | consensus-rnd | 完成判定只认退出码 + sentinel，LLM 伪造不了 |
| grilling（每问附推荐答案） | mattpocock-skills | 澄清意图的提问工艺——推荐答案让用户确认比开放问答快一个量级 |
| seam TDD + 反同义反复测试 | mattpocock-skills | "什么样的测试才算数"——只在预先约定的公共边界写测试，期望值须独立来源 |

---

## 八、结语：一个可以带走的判断标准

十一个仓库读完，所有维度、所有评分最终收束到一条分水岭：**机器强制 vs prompt 自律**。

Prompt 自律不是没用——superpowers 证明了好的 prompt 纪律能覆盖 80% 的日常场景，而且它是唯一能跨 10 个 host 迁移的东西。但 prompt 自律有一个结构性缺陷：它的失效是**静默的**。模型不遵守 Iron Law 的那一次，你不会收到任何告警；hook 拦截失败的那一次，工具调用会直接报错。前者靠运气发现，后者靠机制发现。

所以，评估任何一个 agent workflow——包括你自己正在搭的那个——只需要先问两个问题：

1. **哪些环节是机器强制的？** 把 16 个维度过一遍，标出 L3 的格子。特别看 S5（证据是机器事实还是模型口述）和 C4（有没有 PreToolUse 级拦截）。
2. **完成权在谁手里？** 如果 LLM 能自己写下 "done"，那么无论 prompt 里的纪律写得多么声情并茂，这个 workflow 的可靠性上限就是模型的自律性下限。

其余的一切——规格文档的格式、切片的粒度、审查的轮数——都是在这两个答案确定之后的工程细节。

---

## 附：研究方法说明

本文基于对 11 个开源 agent workflow 框架的逐仓库深读（2026 年中的仓库状态；第 11 个 mattpocock/skills 为成文后追加），每个仓库产出一篇独立研究报告，随后两步归纳：先从失败模式反推能力框架（8 步 × 8 横切 × L0-L3 成熟度，其中 C8 无人值守与多任务并行为成文后与一线使用者讨论追加），再用统一口径对 11 框架做 16 维评分（判级看最弱链，每格评分可回溯到具体机制）。全部中间材料在本文同目录：

- `00-开篇.md`：研究缘由与对象清单
- `01-速览表.md` / `01-仓库研究/`：10 篇逐仓库深读报告
- `02-能力框架.md`：能力框架推导全过程（含候选概念逐条裁决表）
- `03-对比表.md`：完整 10×15 评分矩阵、逐框架评分依据、维度冠军与剖面分析

局限：评分基于仓库文档与代码静读，未对每个框架做同任务实测跑分（仅 Trellis 与 consensus-rnd 有作者本人的 n=1 使用体验作为注脚，见第五节）；框架迭代快，L 级判定以深读时点的版本为准。
