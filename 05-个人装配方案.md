# 个人装配方案 v0.1(host 无关 · 陪伴为主 · 机制层逐点放置控制权)

> 这不是又一个框架,是按 `04-最终文章.md` 的结论,为一个具体的人拼的一套。
> 设计者画像:web 背景、陪伴型开发(建 task→深度讨论→放手执行→参与测试,不亲手写码)、烦重框架黑箱、要可魔改、被重构回归坑过。目标是通用开发环境能用就行。

---

## 一、四个设计前提(你选的)及其推论

| 选择 | 推论(决定了后面怎么设计) |
|---|---|
| **从零按机制拼** | 不继承任何框架的耦合;每个件你能看懂能改;代价是要自己搭骨架 |
| **host 无关** | ⚠️ **放弃 host hook 的"事前拦截"**;强制力全部下沉到 git hook / CI / 文件约定;能力件写成通用 SKILL.md(CC 与 Codex 都能跑) |
| **skill 集 + 少量脚本** | 能力 = 可调用的 SKILL.md;强制 = 少数几个可读的 git hook/脚本(不要黑箱,件数越少越好) |
| **陪伴为主 + 放跑挡位** | 默认走陪伴流(你在环里);放跑是**显式开关**,且只对"需求已冻结 + 机器可验收"的任务开 |

**一句话架构**:能力靠 prompt(可移植),强制靠 git(不可绕、host 无关),控制权按机制逐点放——危险的给机器,判断的留自己。

---

## 二、总装图(8 步 + 谁掌控 + 怎么强制)

| 步骤 | 件(skill/脚本) | 控制权 | 强制机制(host 无关) | 可达 L 级 |
|---|---|---|---|---|
| S1 澄清意图 | `grill` skill | **人** | 批准信号落盘(`.flow/approved`)才准进 S4 | L2 |
| S2 写清规格 | `spec` skill → `spec.md` | 人+机 | 验收标准必须写成可跑命令(格式校验) | L2 |
| S3 小步切片 | `slice` skill → `tasks.md` | 人+机 | 每片"可独立验收"才算合格 | L1+ |
| S4 实现功能 | `implement` skill | **机器执行,架构问人** | 偏差协议(见下);无 `approved` 拒跑 | L1+ |
| S5 验证证据 | 验收命令 + `.flow/evidence/` | **机器** | **git pre-push:命令不绿不许 push** | **L3** |
| S6 独立审查 | `pre-push-review.sh`(跨模型) | **机器(独立)** | pre-push 调另一模型审 diff,BLOCK 拦 | L2 |
| S7 可控发布 | 分支保护 + required checks | **机器(服务端)** | main 禁直推 + 测试挂 required | **L3** |
| S8 复盘沉淀 | `learnings.md` → 毕业进 AGENTS.md | 人+机 | 回写才算完成;沉淀进 AGENTS.md 自动加载 | L2 |

**为什么 S5/S7 能到 L3 而不绑 host**:git pre-push 和服务端分支保护都是 host 无关的——换 Codex、换 opencode 照样生效。这是你"既要可移植又要机器强制"的唯一同时满足点。

---

## 三、逐件清单(每件:干什么 / 抄谁 / 怎么强制)

### 能力件(SKILL.md,可移植,靠 prompt)

- **`grill`(S1)** — 抄 mattpocock 的 grilling:一次一问 + **每问附推荐答案** + "能查代码库回答的绝不问你"。产出共识后写 `.flow/approved`(一个空标记文件 + 一句决议)。*为什么*:对齐是你四大失败模式之首,做好了后面全省。
- **`spec`(S2)** — 抄 missions 的验收合同 + spec-kit 的 converge。产出 `spec.md`:每条验收标准写成 `WHEN 条件 THEN 结果; verify: <可跑命令>`。*关键双用*:这份 spec 同时是你**重构回归的防线**——重构前它就是"行为清单",重构后逐条对照。
- **`slice`(S3)** — 抄 superpowers 的切分标准("reviewer 能否独立批准这一片")+ gsd 的竖切。产出 `tasks.md`,每片带自己的 verify 命令。
- **`implement`(S4)** — 抄 gsd 偏差四规则:①能自动修的直接修 ②架构级变更**必须停下问你** ③3 次尝试上限 ④全部记入 `.flow/summary.md`。*为什么*:你在陌生技术栈上看不懂 diff,"架构问人"是你唯一能兜底的关口,必须留。

### 强制件的接口契约(所有强制脚本遵守)

**核心原则:逻辑与触发器解耦。** 强制件写成"触发器无关的独立可执行",触发器只是薄适配层。

- **输入靠参数,不假设来源**:`verify.sh --range origin/main...HEAD`,核心脚本自己算默认 range,不读 git hook 特有的 stdin(否则手动跑就断)。
- **退出码即结论**:`0=过 / 1=拦 / 2=错`——所有触发器的通用语言。
- **单一职责**:verify / review / secret-scan 各写各的,触发器自由组合。
- **薄适配层**:手动(`./flow verify`)、git hook(`.githooks/pre-push` 一行调用)、host hook(CC 的 hook JSON 指同一脚本)、CI(一个 step 调同一脚本)——每种三五行。

**由此得到的关键性质:L 级是接线决定,不是重写。** 同一个 `verify.sh`,手动跑 = L1(靠自律),接到 pre-push = L3(机器强制)。代码一行不改,接线=升级,不接=退回,还能逐仓库选。

### 强制件(脚本,不可绕,靠 git)

- **`pre-push`(S5+S6,一个 hook 两件事)**:
  1. 跑 `spec.md` 里所有 verify 命令 → 有红即 `exit 1`,push 被挡(**完成权剥夺**:不绿你说完成了也没用)
  2. `git diff origin/main...HEAD` + `spec.md` 喂给另一模型(`codex exec`)审缺漏/糊弄/范围偏离 → 输出含 `BLOCK` 即拦,结论落盘 `reviews/<sha>.md`
  - *工程注意*:挂 pre-push 不挂 pre-commit(别逼死小步提交);审查 AI 超时**放行 + 落盘告警**(别 fail-closed,否则 API 一抖没法 push);测试是硬门,AI 审是"抓缺漏的网"。
- **`state`(C1)** — 抄 web-dev-skills 的无时间戳 `.flow/state.json`:纯 status 枚举 + 文件存在性,任意时刻无状态重建。断线/compaction 后 `recover` skill 读它接上。
- **门禁自测** — 抄 consensus-rnd:`pre-push` 脚本自己要有几个测试用例(改坏了没人知道等于没门禁)。

### 服务端(S7,免费的 L3)

- main 开**分支保护**:禁直推、禁 force push。
- 把测试挂成 **required check**。两分钟配完,agent 物理够不着,是你不写任何代码就白得的最强门禁。

---

## 四、两条通道

### 陪伴通道(默认)
`grill`(讨论到共识→approved)→ `spec` → `slice` → 你放手让它 `implement` → 你参与测试 → `git push`(pre-push 自动验证+跨模型审)→ 合 PR。
**你在环里的两个点**:S1 深度讨论、S4 架构决策被 escalate 时。其余机器跑,门禁兜底。

### 放跑挡位(显式开关,`FLOW_UNATTENDED=1`)
**只对满足三条的任务开**:①需求已冻结(spec.md 定稿) ②任务间零文件交集 ③机器可端到端验收。典型:补测试 / 迁移 API / 修一类 lint。
- 机制:一个 `run-loop.sh` —— `while 未达 spec: implement 一步 → 跑 verify → 绿则标 done;卡 3 次则停下发通知`。**完成态只由脚本按 verify 结果写**(LLM 不能自证完成)。
- ⚠️ **无人值守时护栏要更严,不是更松**:放跑在**独立 worktree** 跑(隔离,不污染主干);破坏性操作(`rm -rf`、force push、删数据)一律停下问。原因——没人盯着,一次越界后面就是几小时无人纠正的狂奔,这正是 C4/C5 从加分项变生死线的场景。

---

### 预设档位(light ↔ max = 接线档位,非多套代码)

因为 L 级已是接线决定,"轻/重"预设就是**同一批脚本装多少触发器**,零重复代码:

| 档位 | 接了什么 | 适合 |
|---|---|---|
| **light** | 什么 hook 都不接,全靠 `./flow` 手动跑 | 改文案、小修——避开流程税(治"小任务也走全流程") |
| **standard** | pre-push(verify+review)+ 分支保护 | 日常 feature,陪伴流默认档 |
| **max** | 上面 + pre-commit(secret/断言防护)+ commit-msg(追溯)+ CI required | 重要模块、放跑挡位、长期仓库 |

切档 = 装/卸几个薄适配层,核心不动。这也是"改文案也走全流程"这个流程税问题的解:按任务选档,不是按框架。

---

## 五、诚实告警:host 无关的两处代价

1. **C4 事前拦截缺失**。host 无关意味着没有 PreToolUse hook,`rm -rf` / force push / 删数据这类**事前**拦截做不了——git hook 只在 commit/push 时才介入,拦不住"还没到 git 就已经删了"的操作。
   - **可选让步方案**(要不要用看你):核心保持 host 无关,但**单独挂一个可拆的 host PreToolUse shim**,只干一件事——拦危险命令。它是 add-on 不进核心、不影响可移植;不用它时,退路是放跑走 worktree 隔离 + git pre-commit 拦部分。通用开发环境下这不是刚需,按需再加。
2. **S8 自动注入弱一档**。trellis 靠 hook 每轮注入拿到 C2=L3,你没 hook。替代:**让 learnings 毕业进 `AGENTS.md`/`CLAUDE.md`**——这俩文件每个 host 启动都自动加载,等于 host 无关的自动注入。代价:注入的是"沉淀后的规则"而非"每轮动态上下文",够用但不如 hook 实时。

---

## 六、落地顺序(MVP 先行,别一次搭全)

| 阶段 | 先做什么 | 为什么这个顺序 |
|---|---|---|
| **MVP** | `pre-push`(验证+跨模型审)+ 服务端分支保护 | 强制层是地基;先有不可绕的门,能力件再烂也不会把 main 弄脏 |
| **第二批** | `grill` + `spec` + `implement` 三个能力件 | 你陪伴流的主干,S1/S2/S4 |
| **第三批** | `state` + `recover`(断点恢复)+ learnings 毕业机制 | 长任务和跨 session 才用得上 |
| **最后** | 放跑挡位 `run-loop.sh` + 危险命令 shim | 前面稳了、你信得过门禁了,再放手无人值守 |

**原则**:每加一件,先问"它把控制权放对地方了吗、它 host 无关吗、它我能看懂能改吗"。三个都是,才进。

---

## 待定 / 下一步

- [ ] `.flow/` 目录结构和 `state.json` 字段定稿
- [ ] `pre-push-review.sh` 的具体脚本(跨模型审那段)先写一版
- [ ] git hook 装配点定稿(pre-commit 放什么轻校验 / pre-push 放什么重门禁 / 用 `core.hooksPath` 让 hook 随仓库走)
- [ ] 放跑挡位的 worktree 隔离细节
